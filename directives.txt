Directive(smcl)
    Directive(comment; *! version VERSION_NUMBER)
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "[R] areg" "help areg")
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "[R] xtreg" "help xtreg")
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "[R] ivregress" "help ivregress")
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "" "--")
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "ivreg2" "help ivreg2")
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "ivregress" "help ivregress")
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "reg2hdfe" "help reg2hdfe")
    Directive(nobreak)
    Directive(newline)
    Directive(vieweralsosee; "a2reg" "help a2reg")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Syntax" "reghdfe##syntax")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Description" "reghdfe##description")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Options" "reghdfe##options")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Postestimation Syntax" "reghdfe##postestimation")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Remarks" "reghdfe##remarks")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Examples" "reghdfe##examples")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Stored results" "reghdfe##results")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Author" "reghdfe##contact")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Updates" "reghdfe##updates")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "Acknowledgements" "reghdfe##acknowledgements")
    Directive(nobreak)
    Directive(newline)
    Directive(viewerjumpto; "References" "reghdfe##references")
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Title")
    Directive(newline)
    Directive(newline)
    Directive(p2colset; 5 18 20 2)
    Directive(nobreak)
    Directive(newline)
    Directive(p2col)
        Directive(cmd)
            Text("reghdfe")
        Text(" ")
        Directive(hline; 2)
    Text("Linear and instrumental-variable/GMM regression absorbing multiple levels of fixed effects")
    Directive(p_end)
    Directive(newline)
    Directive(p2colreset)
    Directive(nobreak)
    Directive(newline)
    Directive(newline)
    Directive(marker; syntax)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Syntax")
    Directive(newline)
    Directive(newline)
    Directive(p; 8 15 2)
    Text(" ")
    Directive(cmd)
        Text("reghdfe")
    Directive(newline)
    Directive(depvar)
    Text(" [")
    Directive(indepvars)
    Text("] [")
    Directive(cmd)
        Text("(")
    Directive(it)
        Directive(help; varlist)
            Text("endogvars")
    Text(" ")
    Directive(cmd)
        Text("=")
    Text(" ")
    Directive(it)
        Directive(help; varlist)
            Text("iv_vars")
    Directive(cmd)
        Text(")")
    Text("]")
    Directive(newline)
    Directive(ifin)
    Text(" ")
    Directive(it)
        Directive(weight)
    Text(" ")
    Directive(cmd)
        Text(",")
    Text(" ")
    Directive(opth; a)
        Text("bsorb(reghdfe##absvar:absvars)")
    Text(" [")
    Directive(help; reghdfe##options)
        Text("options")
    Text("] ")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_summary)
    Directive(nobreak)
    Directive(newline)
    Directive(synoptset; 22 tabbed)
    Directive(nobreak)
    Directive(newline)
    Directive(synopthdr)
    Directive(newline)
    Directive(synoptline)
    Directive(newline)
    Directive(syntab)
        Text("Model ")
        Directive(help; reghdfe##opt_model)
            Text("[+]")
    Directive(newline)
    Directive(p2coldent)
        Text("* ")
        Directive(opth; a)
            Text("bsorb(reghdfe##absvar:absvars)")
    Text("identifiers of the absorbed fixed effects; each ")
    Directive(help; reghdfe##absvar)
        Text("absvar")
    Text(" represents one set of fixed effects")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Text(" ")
        Directive(cmdab)
            Text("a:bsorb(")
        Directive(it)
            Text("...")
        Directive(cmd)
            Text(",")
        Text(" ")
        Directive(cmdab)
            Text("save:fe)")
    Text("save all fixed effect estimates (")
    Directive(it)
        Text("__hdfe*")
    Text(" prefix); useful for a subsequent ")
    Directive(help; reghdfe##postestimation)
        Text("predict")
    Text(".")
    Directive(newline)
    Text("However, see also the ")
    Directive(it)
        Text("resid")
    Text(" option.")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Text(" ")
        Directive(opth; res)
            Text("iduals(newvar)")
    Text("save residuals; more direct and much faster than saving the fixed effects and then running predict")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opth; su)
            Text("mmarize(tabstat##statname:stats)")
    Text("equivalent to ")
    Directive(help; reghdfe##postestimation)
        Text("estat summarize")
    Text(" after the regression,")
    Directive(newline)
    Text("but more flexible, compatible with the ")
    Directive(opt; fast)
    Text(" option, and saves results on ")
    Directive(it)
        Text("e(summarize)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Text(" ")
        Directive(opt; subopt)
            Text("ions(...)")
    Text("additional options that will be passed to the regression command (either ")
    Directive(help; regress)
    Text(", ")
    Directive(help; ivreg2)
    Text(", or ")
    Directive(help; ivregress)
    Text(")")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("SE/Robust ")
        Directive(help; reghdfe##opt_vce)
            Text("[+]")
    Directive(newline)
    Directive(p2coldent)
        Text("+ ")
        Directive(opt; vce)
        Directive(cmd)
            Text("(")
        Directive(help; reghdfe##opt_vce)
            Text("vcetype")
        Text(" [")
        Directive(cmd)
            Text(",")
        Directive(it)
            Text("opt")
        Text("]")
        Directive(cmd)
            Text(")")
    Directive(it)
        Text("vcetype")
    Directive(newline)
    Text("may be ")
    Directive(opt; un)
        Text("adjusted")
    Text(" (default), ")
    Directive(opt; r)
        Text("obust")
    Text(" or ")
    Directive(opt; cl)
        Text("uster")
    Text(" ")
    Directive(help; fvvarlist)
    Text(" (allowing two- and multi-way clustering)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
    Text("suboptions ")
    Directive(opt; bw(#))
    Text(", ")
    Directive(opt; ker)
        Text("nel(str)")
    Text(", ")
    Directive(opt; dkraay(#))
    Text(" and ")
    Directive(opt; kiefer)
    Text(" allow for AC/HAC estimates; see the ")
    Directive(help; avar)
    Text(" package")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("Instrumental-Variable/2SLS/GMM ")
        Directive(help; reghdfe##opt_iv)
            Text("[+]")
    Directive(newline)
    Directive(synopt)
        Directive(opt; est)
            Text("imator(str)")
    Text("either ")
    Directive(opt; 2sls)
    Text(" (default), ")
    Directive(opt; gmm)
        Text("2s")
    Text(" (two-stage GMM),")
    Directive(newline)
    Directive(opt; liml)
    Text(" (limited-information maximum likelihood) or ")
    Directive(opt; cue)
    Text(" (which gives approximate results, see discussion below)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; stage)
            Text("s(list)")
    Text("estimate additional regressions; choose any of ")
    Directive(opt; first)
    Text(" ")
    Directive(opt; ols)
    Text(" ")
    Directive(opt; reduced)
    Text(" ")
    Directive(opt; acid)
    Text(" (or ")
    Directive(opt; all)
    Text(")")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; ff)
            Text("irst")
    Text("compute first-stage diagnostic and identification statistics")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opth; iv)
            Text("suite(subcmd)")
    Text("package used in the IV/GMM regressions;")
    Directive(newline)
    Text("options are ")
    Directive(opt; ivreg2)
    Text(" (default; needs installing) and ")
    Directive(opt; ivregress)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("Diagnostic ")
        Directive(help; reghdfe##opt_diagnostic)
            Text("[+]")
    Directive(newline)
    Directive(synopt)
        Directive(opt; v)
            Text("erbose(#)")
    Text("amount of debugging information to show (0=None, 1=Some, 2=More, 3=Parsing/convergence details, 4=Every iteration)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; time)
            Text("it")
    Text("show elapsed times by stage of computation")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("Optimization ")
        Directive(help; reghdfe##opt_optimization)
            Text("[+]")
    Directive(newline)
    Directive(p2coldent)
        Text("+ ")
        Directive(opth; tol)
            Text("erance(#)")
    Text("criterion for convergence (default=1e-8)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opth; maxit)
            Text("erations(#)")
    Text("maximum number of iterations (default=10,000); if set to missing (")
    Directive(cmd)
        Text(".")
    Text(") it will run for as long as it takes.")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opth; pool)
            Text("size(#)")
    Text("apply the within algorithm in groups of ")
    Directive(it)
        Text("#")
    Text(" variables (default 10). a large poolsize is usually faster but uses more memory")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; accel)
            Text("eration(str)")
    Text("acceleration method; options are conjugate_gradient (cg), steep_descent (sd), aitken (a), and none (no)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; transf)
            Text("orm(str)")
    Text("transform operation that defines the type of alternating projection; options are Kaczmarz (kac), Cimmino (cim), Symmetric Kaczmarz (sym)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("Speedup Tricks ")
        Directive(help; reghdfe##opt_speedup)
            Text("[+]")
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text(" cache(save")
        Text(" [,opt]")
        Directive(cmd)
            Text(")")
    Text("absorb all variables without regressing (destructive; combine it with ")
    Directive(help; preserve)
        Text("preserve/restore")
    Text(")")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
    Text("suboption ")
    Directive(opth; keep(varlist))
    Text(" adds additional untransformed variables to the resulting dataset")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text(" cache(use)")
    Text("run regressions on cached data; ")
    Directive(it)
        Text("vce()")
    Text(" must be the same as with ")
    Directive(cmd)
        Text(" cache(save)")
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text(" cache(clear)")
    Text("delete Mata objects to clear up memory; no more regressions can be run after this")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; fast)
    Text("will not create ")
    Directive(it)
        Text("e(sample)")
    Text("; disabled when saving fixed effects, residuals or mobility groups")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("Degrees-of-Freedom Adjustments ")
        Directive(help; reghdfe##opt_dof)
            Text("[+]")
    Directive(newline)
    Directive(synopt)
        Directive(opt; dof)
            Text("adjustments(list)")
    Text("allows selecting the desired adjustments for degrees of freedom;")
    Directive(newline)
    Text("rarely used")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Text(" ")
        Directive(opth; groupv)
            Text("ar(newvar)")
    Text("unique identifier for the first mobility group")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("Reporting ")
        Directive(help; reghdfe##opt_reporting)
            Text("[+]")
    Directive(newline)
    Directive(synopt)
        Directive(opt; version)
    Text("reports the version number and date of reghdfe, and saves it in e(version). standalone option")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; l)
            Text("evel(#)")
    Text("set confidence level; default is ")
    Directive(cmd)
        Text("level(95)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(it)
            Directive(help; reghdfe##display_options)
                Text("display_options")
    Text("control column formats, row spacing, line width, display of omitted variables and base and empty cells, and factor-variable labeling.")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
    Text("particularly useful are the ")
    Directive(opt; noomit)
        Text("ted")
    Text(" and ")
    Directive(opt; noempty)
    Text(" options to hide regressors omitted due to collinearity")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(syntab)
        Text("Undocumented")
    Directive(newline)
    Directive(synopt)
        Directive(opt; keepsin)
            Text("gletons")
    Text("do not drop singleton groups")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(opt; old)
    Text("will call the latest 2.x version of reghdfe instead (see the ")
    Directive(help; reghdfe_old)
        Text("old help file")
    Text(")")
    Directive(p_end)
    Directive(newline)
    Directive(synoptline)
    Directive(newline)
    Directive(p2colreset)
    Directive(nobreak)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("* ")
    Directive(opt; absorb(absvars))
    Text(" is required.")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("+ indicates a recommended or important option.")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Directive(it)
        Text("indepvars")
    Text(", ")
    Directive(it)
        Text("endogvars")
    Text(" and ")
    Directive(it)
        Text("iv_vars")
    Text(" may contain factor variables; see ")
    Directive(help; fvvarlist)
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("all the regression variables may contain time-series operators; see ")
    Directive(help; tsvarlist)
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Directive(cmd)
        Text("fweight")
    Text("s, ")
    Directive(cmd)
        Text("aweight")
    Text("s and ")
    Directive(cmd)
        Text("pweight")
    Text("s are allowed; see ")
    Directive(help; weight)
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(newline)
    Directive(marker; absvar)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Absvar Syntax")
    Directive(newline)
    Directive(newline)
    Directive(synoptset; 22)
    Directive(nobreak)
    Directive(newline)
    Directive(synopthdr)
        Text("absvar")
    Directive(newline)
    Directive(synoptline)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("i.")
        Directive(it)
            Text("varname")
    Text("categorical variable to be absorbed (the ")
    Directive(cmd)
        Text("i.")
    Text(" prefix is tacit)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("i.")
        Directive(it)
            Text("var1")
        Directive(cmd)
            Text("#i.")
        Directive(it)
            Text("var2")
    Text("absorb the interactions of multiple categorical variables")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("i.")
        Directive(it)
            Text("var1")
        Directive(cmd)
            Text("#")
        Directive(cmd)
            Text("c.")
        Directive(it)
            Text("var2")
    Text("absorb heterogeneous slopes, where ")
    Directive(it)
        Text("var2")
    Text(" has a different slope coef. depending on the category of ")
    Directive(it)
        Text("var1")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(it)
            Text("var1")
        Directive(cmd)
            Text("##")
        Directive(cmd)
            Text("c.")
        Directive(it)
            Text("var2")
    Text("equivalent to "")
    Directive(cmd)
        Text("i.")
    Directive(it)
        Text("var1")
    Text(" ")
    Directive(cmd)
        Text("i.")
    Directive(it)
        Text("var1")
    Directive(cmd)
        Text("#")
    Directive(cmd)
        Text("c.")
    Directive(it)
        Text("var2")
    Text("", but ")
    Directive(it)
        Text("much")
    Text(" faster")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(it)
            Text("var1")
        Directive(cmd)
            Text("##c.(")
        Directive(it)
            Text("var2 var3")
        Directive(cmd)
            Text(")")
    Text("multiple heterogeneous slopes are allowed together. Alternative syntax: ")
    Directive(it)
        Text("var1")
    Directive(cmd)
        Text("##(c.")
    Directive(it)
        Text("var2")
    Text(" ")
    Directive(cmd)
        Text("c.")
    Directive(it)
        Text("var3")
    Directive(cmd)
        Text(")")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(it)
            Text("v1")
        Directive(cmd)
            Text("#")
        Directive(it)
            Text("v2")
        Directive(cmd)
            Text("#")
        Directive(it)
            Text("v3")
        Directive(cmd)
            Text("##c.(")
        Directive(it)
            Text("v4 v5")
        Directive(cmd)
            Text(")")
    Text("factor operators can be combined")
    Directive(p_end)
    Directive(newline)
    Directive(synoptline)
    Directive(newline)
    Directive(p2colreset)
    Directive(nobreak)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("To save the estimates specific absvars, write ")
    Directive(newvar)
    Directive(inp)
        Text("=")
        Directive(it)
            Text("absvar")
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("Please be aware that in most cases these estimates are neither consistent nor econometrically identified.")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("Using categorical interactions (e.g. ")
    Directive(it)
        Text("x")
    Directive(cmd)
        Text("#")
    Directive(it)
        Text("z")
    Text(") is faster than running ")
    Directive(it)
        Text("egen group(...)")
    Text(" beforehand.")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("Singleton obs. are dropped iteratively until no more singletons are found (see ancilliary article for details).")
    Directive(p_end)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("Slope-only absvars ("state#c.time") have poor numerical stability and slow convergence.")
    Directive(newline)
    Text("If you need those, either i) increase tolerance or")
    Directive(newline)
    Text("ii) use slope-and-intercept absvars ("state##c.time"), even if the intercept is redundant.")
    Directive(newline)
    Text("For instance if absvar is "i.zipcode i.state##c.time" then i.state is redundant given i.zipcode, but")
    Directive(newline)
    Text("convergence will still be ")
    Directive(it)
        Text("much")
    Text(" faster.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; description)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Description")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Directive(cmd)
        Text("reghdfe")
    Text(" is a generalization of ")
    Directive(help; areg)
    Text(" (and ")
    Directive(help; xtreg)
        Text("xtreg,fe")
    Text(", ")
    Directive(help; xtivreg)
        Text("xtivreg,fe")
    Text(") for multiple levels of fixed effects")
    Directive(newline)
    Text("(including heterogeneous slopes), alternative estimators (2sls, gmm2s, liml), and additional robust standard errors (multi-way clustering, HAC standard errors, etc).")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Additional features include:")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 6 9 9 2)
        Text(" a)")
    Text("A novel and robust algorithm to efficiently absorb the fixed effects (extending the work of Guimaraes and Portugal, 2010).")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 6 9 9 2)
        Text(" b)")
    Text("Coded in Mata, which in most scenarios makes it even faster than ")
    Directive(it)
        Text("areg")
    Text(" and ")
    Directive(it)
        Text("xtreg")
    Text(" for a single fixed effect (see benchmarks on the Github page).")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 6 9 9 2)
        Text(" c)")
    Text("Can save the point estimates of the fixed effects (")
    Directive(it)
        Text("caveat emptor")
    Text(": the fixed effects may not be identified, see the ")
    Directive(help; reghdfe##references)
        Text("references")
    Text(").")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 6 9 9 2)
        Text(" d)")
    Text("Calculates the degrees-of-freedom lost due to the fixed effects")
    Directive(newline)
    Text("(note: beyond two levels of fixed effects, this is still an open problem, but we provide a conservative approximation).")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 6 9 9 2)
        Text(" e)")
    Text("Iteratively removes singleton groups by default, to avoid biasing the standard errors (see ancillary document).")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; options)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Options")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_model)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("Model and Miscellanea")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; a)
        Text("bsorb(reghdfe##absvar:absvars)")
    Text(" list of categorical variables (or interactions) representing the fixed effects to be absorbed.")
    Directive(newline)
    Text("this is equivalent to including an indicator/dummy variable for each category of each ")
    Directive(it)
        Text("absvar")
    Text(". ")
    Directive(cmd)
        Text("absorb()")
    Text(" is required.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("To save a fixed effect, prefix the absvar with "")
    Directive(newvar)
    Directive(cmd)
        Text("=")
    Text("".")
    Directive(newline)
    Text("For instance, the option ")
    Directive(input)
        Text("absorb(firm_id worker_id year_coefs=year_id)")
    Text(" will include firm,")
    Directive(newline)
    Text("worker and year fixed effects, but will only save the estimates for the year fixed effects (in the new variable ")
    Directive(it)
        Text("year_coefs")
    Text(").")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("If you want to ")
    Directive(help; reghdfe##postestimation)
        Text("predict")
    Text(" afterwards but don't care about setting the names of each fixed effect, use the ")
    Directive(cmdab)
        Text("save:fe")
    Text(" suboption.")
    Directive(newline)
    Text("This will delete all variables named ")
    Directive(it)
        Text("__hdfe*__")
    Text(" and create new ones as required.")
    Directive(newline)
    Text("Example: ")
    Directive(it)
        Text("reghdfe price weight, absorb(turn trunk, savefe)")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; res)
        Text("iduals(newvar)")
    Text(" will save the regression residuals in a new variable.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("This is a superior alternative than running ")
    Directive(cmd)
        Text("predict, resid")
    Text(" afterwards as it's faster and doesn't require saving the fixed effects.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; su)
        Text("mmarize(tabstat##statname:stats)")
    Text(" will report and save a table of summary of statistics of the regression")
    Directive(newline)
    Text("variables (including the instruments, if applicable), using the same sample as the regression.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text(" ")
    Directive(opt; su)
        Text("mmarize")
    Text(" (without parenthesis) saves the default set of statistics: ")
    Directive(it)
        Text("mean min max")
    Text(".")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text(" The complete list of accepted statistics is available in the ")
    Directive(help; tabstat##statname)
        Text("tabstat help")
    Text(". The most useful are ")
    Directive(it)
        Text("count range sd median p##")
    Text(".")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text(" The summary table is saved in ")
    Directive(it)
        Text("e(summarize)")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text(" To save the summary table silently (without showing it after the regression table), use the ")
    Directive(opt; qui)
        Text("etly")
    Text(" suboption. You can use it by itself (")
    Directive(cmd)
        Text("summarize(,quietly)")
    Text(") or with custom statistics (")
    Directive(cmd)
        Text("summarize(mean, quietly)")
    Text(").")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; subopt)
        Text("ions(...)")
    Directive(newline)
    Text("options that will be passed directly to the regression command (either ")
    Directive(help; regress)
    Text(", ")
    Directive(help; ivreg2)
    Text(", or ")
    Directive(help; ivregress)
    Text(")")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_vce)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("SE/Robust")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; vce)
        Text("(reghdfe##vcetype:vcetype, subopt)")
    Directive(newline)
    Text("specifies the type of standard error reported.")
    Directive(newline)
    Text("Note that all the advanced estimators rely on asymptotic theory, and will likely have poor performance with small samples")
    Directive(newline)
    Text("(but again if you are using reghdfe, that is probably not your case)")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; un)
        Text("adjusted")
    Text("/")
    Directive(opt; ols)
    Text(" estimates conventional standard errors, valid even in small samples")
    Directive(newline)
    Text("under the assumptions of homoscedasticity and no correlation between observations")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; r)
        Text("obust")
    Text(" estimates heteroscedasticity-consistent standard errors (Huber/White/sandwich estimators), but still assuming independence between observations")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text("Warning: in a FE panel regression, using ")
    Directive(opt; r)
        Text("obust")
    Text(" will")
    Directive(newline)
    Text("lead to inconsistent standard errors if for every fixed effect, the ")
    Directive(it)
        Text("other")
    Text(" dimension is fixed.")
    Directive(newline)
    Text("For instance, in an standard panel with individual and time fixed effects, we require both the number of")
    Directive(newline)
    Text("individuals and time periods to grow asymptotically.")
    Directive(newline)
    Text("If that is not the case, an alternative may be to use clustered errors,")
    Directive(newline)
    Text("which as discussed below will still have their own asymptotic requirements.")
    Directive(newline)
    Text("For a discussion, see")
    Directive(newline)
    Directive(browse; "http)
        Text("//www.princeton.edu/~mwatson/papers/ecta6489.pdf":Stock and Watson, "Heteroskedasticity-robust standard errors for fixed-effects panel-data regression," Econometrica 76 (2008): 155-174")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; cl)
        Text("uster")
    Text(" ")
    Directive(it)
        Text("clustervars")
    Text(" estimates consistent standard errors even when the observations")
    Directive(newline)
    Text("are correlated within groups.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Multi-way-clustering is allowed. Thus, you can indicate as many ")
    Directive(it)
        Text("clustervar")
    Text("s as desired")
    Directive(newline)
    Text("(e.g. allowing for intragroup correlation across individuals, time, country, etc).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Each ")
    Directive(it)
        Text("clustervar")
    Text(" permits interactions of the type ")
    Directive(it)
        Text("var1")
        Directive(cmd)
            Text("#")
        Text("var2")
    Directive(newline)
    Text("(this is faster than using ")
    Directive(cmd)
        Text("egen group()")
    Text(" for a one-off regression).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text(" Warning: The number of clusters, for all of the cluster variables, must go off to infinity.")
    Directive(newline)
    Text("A frequent rule of thumb is that each cluster variable must have at least 50 different categories")
    Directive(newline)
    Text("(the number of categories for each clustervar appears on the header of the regression table).")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Text("The following suboptions require either the ")
    Directive(help; ivreg2)
    Text(" or the ")
    Directive(help; avar)
    Text(" package from SSC.")
    Directive(newline)
    Text("For a careful explanation, see the ")
    Directive(help; ivreg2##s_robust)
        Text("ivreg2 help file")
    Text(", from which the comments below borrow.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; u)
        Text("nadjusted")
    Directive(cmd)
        Text(", ")
    Directive(opt; bw(#))
    Text(" (or just ")
    Directive(cmd)
        Text(", ")
    Directive(opt; bw(#))
    Text(") estimates autocorrelation-consistent standard errors (Newey-West).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; r)
        Text("obust")
    Directive(cmd)
        Text(", ")
    Directive(opt; bw(#))
    Text(" estimates autocorrelation-and-heteroscedasticity consistent standard errors (HAC).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; cl)
        Text("uster")
    Text(" ")
    Directive(it)
        Text("clustervars")
    Directive(cmd)
        Text(", ")
    Directive(opt; bw(#))
    Text(" estimates standard errors consistent to common autocorrelated disturbances (Driscoll-Kraay). At most two cluster variables can be used in this case.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(cmd)
        Text(", ")
    Directive(opt; kiefer)
    Text(" estimates standard errors consistent under arbitrary intra-group autocorrelation (but not heteroskedasticity) (Kiefer).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; kernel(str))
    Text(" is allowed in all the cases that allow ")
    Directive(opt; bw(#))
    Directive(newline)
    Text("The default kernel is ")
    Directive(it)
        Text("bar")
    Text(" (Bartlett). Valid kernels are Bartlett (bar); Truncated (tru); Parzen (par);")
    Directive(newline)
    Text("Tukey-Hanning (thann); Tukey-Hamming (thamm); Daniell (dan); Tent (ten); and Quadratic-Spectral (qua or qs).")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Text("Advanced suboptions:")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(cmd)
        Text(", ")
    Directive(opt; suite(default|mwc|avar))
    Text(" overrides the package chosen by reghdfe to estimate the VCE.")
    Directive(newline)
    Directive(it)
        Text("default")
    Text(" uses the default Stata computation (allows unadjusted, robust, and at most one cluster variable).")
    Directive(newline)
    Directive(it)
        Text("mwc")
    Text(" allows multi-way-clustering (any number of cluster variables), but without the ")
    Directive(it)
        Text("bw")
    Text(" and ")
    Directive(it)
        Text("kernel")
    Text(" suboptions.")
    Directive(newline)
    Directive(it)
        Text("avar")
    Text(" uses the avar package from SSC. Is the same package used by ivreg2, and allows the ")
    Directive(it)
        Text("bw")
    Text(", ")
    Directive(it)
        Text("kernel")
    Text(", ")
    Directive(it)
        Text("dkraay")
    Text(" and ")
    Directive(it)
        Text("kiefer")
    Text(" suboptions.")
    Directive(newline)
    Text("This is useful almost exclusively for debugging.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(cmd)
        Text(", ")
    Directive(opt; twice)
        Text("robust")
    Text(" will compute robust standard errors not only on the first but on the second step of the gmm2s estimation. Requires ")
    Directive(opt; ivsuite(ivregress))
    Text(", but will not give the exact same results as ivregress.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(it)
        Text("Explanation:")
    Text(" When running instrumental-variable regressions with the ")
    Directive(cmd)
        Text("ivregress")
    Text(" package,")
    Directive(newline)
    Text("robust standard errors, and a gmm2s estimator, reghdfe will translate")
    Directive(newline)
    Directive(opt; vce(robust))
    Text(" into ")
    Directive(opt; wmatrix(robust))
    Text(" ")
    Directive(opt; vce(unadjusted))
    Text(".")
    Directive(newline)
    Text("This maintains compatibility with ")
    Directive(cmd)
        Text("ivreg2")
    Text(" and other packages, but may unadvisable as described in ")
    Directive(help; ivregress)
    Text(" (technical note). Specifying this option will instead use ")
    Directive(opt; wmatrix(robust))
    Text(" ")
    Directive(opt; vce(robust))
    Text(".")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text("However, computing the second-step vce matrix requires computing updated estimates (including updated fixed effects).")
    Directive(newline)
    Text("Since reghdfe currently does not allow this, the resulting standard errors")
    Directive(newline)
    Directive(hi)
        Text("will not be exactly the same as with ivregress")
    Text(".")
    Directive(newline)
    Text("This issue is similar to applying the CUE estimator, described further below.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text("Note: The above comments are also appliable to clustered standard error.")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_iv)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("IV/2SLS/GMM")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; est)
        Text("imator")
    Directive(cmd)
        Text("(")
    Directive(opt; 2sls)
    Text("|")
    Directive(opt; gmm)
        Text("2s")
    Text("|")
    Directive(opt; liml)
    Text("|")
    Directive(opt; cue)
    Directive(cmd)
        Text(")")
    Directive(newline)
    Text("estimator used in the instrumental-variable estimation")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; 2sls)
    Text(" (two-stage least squares, default), ")
    Directive(opt; gmm)
        Text("2s")
    Text(" (two-stage efficient GMM), ")
    Directive(opt; liml)
    Text(" (limited-information maximum likelihood), and")
    Directive(newline)
    Directive(opt; cue)
    Text(" ("continuously-updated" GMM) are allowed.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Warning: ")
    Directive(opt; cue)
    Text(" will not give the same results as ivreg2. See the discussion in")
    Directive(newline)
    Directive(browse; "http)
        Text("//www.stata-journal.com/sjpdf.html?articlenum=st0030_3": Baum, Christopher F., Mark E. Schaffer, and Steven Stillman. "Enhanced routines for instrumental variables/GMM estimation and testing." Stata Journal 7.4 (2007): 465-506")
    Directive(newline)
    Text("(page 484).")
    Directive(newline)
    Text("Note that even if this is not exactly ")
    Directive(opt; cue)
    Text(", it may still be a desirable/useful alternative to standard cue, as explained in the article.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; stage)
        Text("s(list)")
    Directive(newline)
    Text("adds and saves up to four auxiliary regressions useful when running instrumental-variable regressions:")
    Directive(newline)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text("first")
    Text(" all first-stage regressions")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text("ols")
    Text(" ols regression (between dependent variable and endogenous variables; useful as a benchmark)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text("reduced")
    Text(" reduced-form regression (ols regression with included and excluded instruments as regressors)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text("acid")
    Text(" an "acid" regression that includes both instruments and endogenous variables as regressors; in this setup, excluded instruments should not be significant.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("You can pass suboptions not just to the iv command but to all stage regressions with a comma after the list of stages. Example:")
    Directive(break)
    Directive(newline)
    Directive(cmd)
        Text("reghdfe price (weight=length), absorb(turn) subopt(nocollin) stages(first, eform(exp(beta)) )")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("By default all stages are saved (see ")
    Directive(help; estimates dir)
    Text(").")
    Directive(newline)
    Text("The suboption ")
    Directive(cmd)
        Text(",nosave")
    Text(" will prevent that.")
    Directive(newline)
    Text("However, future ")
    Directive(cmd)
        Text("replay")
    Text("s will only replay the iv regression.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; ffirst)
    Directive(newline)
    Text("compute and report first stage statistics (")
    Directive(help; ivreg2##s_relevance)
        Text("details")
    Text("); requires the ivreg2 package.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("These statistics will be saved on the ")
    Directive(it)
        Text("e(first)")
    Text(" matrix.")
    Directive(newline)
    Text("If the first-stage estimates are also saved (with the ")
    Directive(cmd)
        Text("stages()")
    Text(" option), the respective statistics will be copied to ")
    Directive(cmd)
        Text("e(first_*)")
    Text(".")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; iv)
        Text("suite(subcmd)")
    Directive(newline)
    Text("allows the IV/2SLS regression to be run either using ")
    Directive(opt; ivregress)
    Text(" or ")
    Directive(opt; ivreg2)
    Text(".")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text(" ")
    Directive(opt; ivreg2)
    Text(" is the default, but needs to be installed for that option to work.")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_diagnostic)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("Diagnostic")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; v)
        Text("erbose(#)")
    Text(" orders the command to print debugging information.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Possible values are 0 (none), 1 (some information), 2 (even more), 3 (adds dots for each iteration, and reportes parsing details), 4 (adds details for every iteration step)")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("For debugging, the most useful value is 3. For simple status reports, set verbose to 1.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; time)
        Text("it")
    Text(" shows the elapsed time at different steps of the estimation. Most time is usually spent on three steps: map_precompute(), map_solve() and the regression step.")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_dof)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("Degrees-of-Freedom Adjustments")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; dof)
        Text("adjustments(doflist)")
    Text(" selects how the degrees-of-freedom, as well as e(df_a), are adjusted due to the absorbed fixed effects.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Without any adjustment, we would assume that the degrees-of-freedom used by the fixed effects is equal to the count of all the fixed effects")
    Directive(newline)
    Text("(e.g. number of individuals + number of years in a typical panel).")
    Directive(newline)
    Text("However, in complex setups (e.g. fixed effects by individual, firm, job position, and year),")
    Directive(newline)
    Text("there may be a huge number of fixed effects collinear with each other, so we want to adjust for that.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Note: changing the default option is rarely needed, except in benchmarks, and to obtain a marginal speed-up by excluding the ")
    Directive(opt; pair)
        Text("wise")
    Text(" option.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; all)
    Text(" is the default and almost always the best alternative. It is equivalent to ")
    Directive(opt; dof(pairwise clusters continuous))
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; none)
    Text(" assumes no collinearity across the fixed effects (i.e. no redundant fixed effects). This is overtly conservative, although it is the faster method by virtue of not doing anything.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; first)
        Text("pair")
    Text(" will exactly identify the number of collinear fixed effects across the first two sets of fixed effects")
    Directive(newline)
    Text("(i.e. the first absvar and the second absvar).")
    Directive(newline)
    Text("The algorithm used for this is described in Abowd et al (1999), and relies on results from graph theory")
    Directive(newline)
    Text("(finding the number of connected sub-graphs in a bipartite graph).")
    Directive(newline)
    Text("It will not do anything for the third and subsequent sets of fixed effects.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("For more than two sets of fixed effects, there are no known results that provide exact degrees-of-freedom as in the case above.")
    Directive(newline)
    Text("One solution is to ignore subsequent fixed effects (and thus oversestimate e(df_a) and understimate the degrees-of-freedom).")
    Directive(newline)
    Text("Another solution, described below, applies the algorithm between pairs of fixed effects to obtain a better (but not exact) estimate:")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; pair)
        Text("wise")
    Text(" applies the aforementioned connected-subgraphs algorithm between pairs of fixed effects.")
    Directive(newline)
    Text("For instance, if there are four sets of FEs, the first dimension will usually have no redundant coefficients (i.e. e(M1)==1), since we are running the model without a constant.")
    Directive(newline)
    Text("For the second FE, the number of connected subgraphs with respect to the first FE will provide an exact estimate of the degrees-of-freedom lost, e(M2).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("For the third FE, we do not know exactly.")
    Directive(newline)
    Text("However, we can compute the number of connected subgraphs between the first and third ")
    Directive(it)
        Text("G(1,3)")
    Text(",")
    Directive(newline)
    Text("and second and third ")
    Directive(it)
        Text("G(2,3)")
    Text(" fixed effects, and choose the higher of those as the closest estimate for e(M3).")
    Directive(newline)
    Text("For the fourth FE, we compute ")
    Directive(it)
        Text("G(1,4)")
    Text(", ")
    Directive(it)
        Text("G(2,4)")
    Text(" and ")
    Directive(it)
        Text("G(3,4)")
    Text(" and again choose the highest for e(M4).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Finally, we compute e(df_a) = e(K1) - e(M1) + e(K2) - e(M2) + e(K3) - e(M3) + e(K4) - e(M4);")
    Directive(newline)
    Text("where e(K#) is the number of levels or dimensions for the #-th fixed effect (e.g. number of individuals or years).")
    Directive(newline)
    Text("Note that e(M3) and e(M4) are only conservative estimates and thus we will usually be overestimating the standard errors. However, given the sizes of the datasets typically used with reghdfe, the difference should be small.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Since the gain from ")
    Directive(opt; pair)
        Text("wise")
    Text(" is usually ")
    Directive(it)
        Text("minuscule")
    Text(" for large datasets, and the computation is expensive, it may be a good practice to exclude this option for speedups.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; cl)
        Text("usters")
    Directive(newline)
    Text("will check if a fixed effect is nested within a ")
    Directive(it)
        Text("clustervar")
    Text(".")
    Directive(newline)
    Text("In that case, it will set e(K#)==e(M#) and no degrees-of-freedom will be lost due to this fixed effect.")
    Directive(newline)
    Text("The rationale is that we are already assuming that the number of effective observations is the number of cluster levels.")
    Directive(newline)
    Text("This is the same adjustment that ")
    Directive(cmd)
        Text("xtreg, fe")
    Text(" does, but ")
    Directive(cmd)
        Text("areg")
    Text(" does not use it.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Directive(opt; cont)
        Text("inuous")
    Directive(newline)
    Text("Fixed effects with continuous interactions (i.e. individual slopes, instead of individual intercepts) are dealt with differently.")
    Directive(newline)
    Text("In an i.categorical#c.continuous interaction, we will do one check: we count the number of categories where c.continuous is always zero.")
    Directive(newline)
    Text("In an i.categorical##c.continuous interaction, we do the above check but replace zero for any particular constant.")
    Directive(newline)
    Text("In the case where continuous is constant for a level of categorical, we know it is collinear with the intercept, so we adjust for it.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Additional methods, such as ")
    Directive(opt; bootstrap)
    Text(" are also possible but not yet implemented.")
    Directive(newline)
    Text("Some preliminary simulations done by the author showed a very poor convergence of this method.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; groupv)
        Text("ar(newvar)")
    Text(" name of the new variable that will contain the first mobility group.")
    Directive(newline)
    Text("Requires ")
    Directive(opt; pair)
        Text("wise")
    Text(", ")
    Directive(opt; first)
        Text("pair")
    Text(", or the default ")
    Directive(opt; all)
    Text(".")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_speedup)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("Speeding Up Estimation")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(cmd)
        Text("reghdfe")
    Text(" ")
    Directive(varlist)
    Text(" ")
    Directive(ifin)
    Directive(cmd)
        Text(",")
    Text(" ")
    Directive(opt; a)
        Text("bsorb(absvars)")
    Text(" ")
    Directive(cmd)
        Text("save(cache)")
    Text(" [")
    Directive(it)
        Text("options")
    Text("]")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("This will transform ")
    Directive(it)
        Text("varlist")
    Text(", absorbing the fixed effects indicated by ")
    Directive(it)
        Text("absvars")
    Text(".")
    Directive(newline)
    Text("It is useful when running a series of alternative specifications with common variables, as the variables will only be transformed once instead of every time a regression is run.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("It replaces the current dataset, so it is a good idea to precede it with a ")
    Directive(help; preserve)
    Text(" command")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("To keep additional (untransformed) variables in the new dataset, use the ")
    Directive(opth; keep(varlist))
    Text(" suboption.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(cmd)
        Text("cache(use)")
    Text(" is used when running reghdfe after a ")
    Directive(it)
        Text("save(cache)")
    Text(" operation. Both the ")
    Directive(it)
        Text("absorb()")
    Text(" and ")
    Directive(it)
        Text("vce()")
    Text(" options must be the same as when the cache was created (the latter because the degrees of freedom were computed at that point).")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(cmd)
        Text("cache(clear)")
    Text(" will delete the Mata objects created by ")
    Directive(it)
        Text("reghdfe")
    Text(" and kept in memory after the ")
    Directive(it)
        Text("save(cache)")
    Text(" operation. These objects may consume a lot of memory, so it is a good idea to clean up the cache. Additionally, if you previously specified ")
    Directive(it)
        Text("preserve")
    Text(", it may be a good time to ")
    Directive(it)
        Text("restore")
    Text(".")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text("Example:")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". sysuse auto")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". preserve")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". * Save the cache")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight length, a(turn rep) vce(turn) cache(save, keep(foreign))")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". * Run regressions")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight, a(turn rep) cache(use)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price length, a(turn rep) cache(use)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". * Clean up")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe, cache(clear)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". restore")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; fast)
    Text(" avoids saving ")
    Directive(it)
        Text("e(sample)")
    Text(" into the regression.")
    Directive(newline)
    Text("Since saving the variable only involves copying a Mata vector, the speedup is currently quite small.")
    Directive(newline)
    Text("Future versions of reghdfe may change this as features are added.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Note that ")
    Directive(opt; fast)
    Text(" will be disabled when adding variables to the dataset (i.e. when saving residuals, fixed effects, or mobility groups), and is incompatible with most postestimation commands.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("If you wish to use ")
    Directive(opt; fast)
    Text(" while reporting ")
    Directive(cmd)
        Text("estat summarize")
    Text(", see the ")
    Directive(opt; summarize)
    Text(" option.")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_optimization)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("Optimization")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; tol)
        Text("erance(#)")
    Text(" specifies the tolerance criterion for convergence; default is ")
    Directive(cmd)
        Text("tolerance(1e-8)")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Note that for tolerances beyond 1e-14, the limits of the ")
    Directive(it)
        Text("double")
    Text(" precision are reached and the results will most likely not converge.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("At the other end, is not tight enough, the regression may not identify perfectly collinear regressors. However, those cases can be easily spotted due to their extremely high standard errors.")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Warning: when absorbing heterogeneous slopes without the accompanying heterogeneous intercepts, convergence is quite poor and a tight tolerance is strongly suggested (i.e. higher than the default). In other words, an absvar of ")
    Directive(it)
        Text("var1##c.var2")
    Text(" converges easily, but an absvar of ")
    Directive(it)
        Text("var1#c.var2")
    Text(" will converge slowly and may require a tighter tolerance.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; maxit)
        Text("erations(#)")
    Directive(newline)
    Text("specifies the maximum number of iterations; the default is ")
    Directive(cmd)
        Text("maxiterations(10000)")
    Text("; set it to missing (")
    Directive(cmd)
        Text(".")
    Text(") to run forever until convergence.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opth; pool)
        Text("size(#)")
    Directive(newline)
    Text("Number of variables that are ")
    Directive(it)
        Text("pooled together")
    Text(" into a matrix that will then be transformed.")
    Directive(newline)
    Text("The default is to pool variables in groups of 5. Larger groups are faster with more than one processor, but may cause out-of-memory errors. In that case, set poolsize to 1.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(it)
        Text("Advanced options:")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; acceleration(str))
    Text(" allows for different acceleration techniques, from the simplest case of")
    Directive(newline)
    Text("no acceleration (")
    Directive(opt; no)
        Text("ne")
    Text("), to steep descent (")
    Directive(opt; st)
        Text("eep_descent")
    Text(" or ")
    Directive(opt; sd)
    Text("), Aitken (")
    Directive(opt; a)
        Text("itken")
    Text("),")
    Directive(newline)
    Text("and finally Conjugate Gradient (")
    Directive(opt; co)
        Text("njugate_gradient")
    Text(" or ")
    Directive(opt; cg)
    Text(").")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Note: Each acceleration is just a plug-in Mata function, so a larger number of acceleration techniques are available, albeit undocumented (and slower).")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; transf)
        Text("orm(str)")
    Text(" allows for different "alternating projection" transforms. The classical transform is Kaczmarz (")
    Directive(opt; kac)
        Text("zmarz")
    Text("), and more stable alternatives are Cimmino (")
    Directive(opt; cim)
        Text("mino")
    Text(") and Symmetric Kaczmarz (")
    Directive(opt; sym)
        Text("metric_kaczmarz")
    Text(")")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Note: Each transform is just a plug-in Mata function, so a larger number of acceleration techniques are available, albeit undocumented (and slower).")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Directive(newline)
    Text("Note: The default acceleration is Conjugate Gradient and the default transform is Symmetric Kaczmarz. Be wary that different accelerations often work better with certain transforms. For instance, do not use conjugate gradient with plain Kaczmarz, as it will not converge.")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; precondition)
    Text(" ")
    Directive(it)
        Text("(currently disabled)")
    Directive(newline)
    Directive(newline)
    Directive(marker; opt_reporting)
    Directive(nobreak)
    Directive(newline)
    Directive(dlgtab)
        Text("Reporting")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(opt; l)
        Text("evel(#)")
    Text(" sets confidence level; default is ")
    Directive(cmd)
        Text("level(95)")
    Directive(newline)
    Directive(newline)
    Directive(marker; display_options)
    Directive(nobreak)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Directive(it)
        Text("display_options")
    Text(":")
    Directive(newline)
    Directive(opt; noomit)
        Text("ted")
    Text(",")
    Directive(newline)
    Directive(opt; vsquish)
    Text(",")
    Directive(newline)
    Directive(opt; noempty)
        Text("cells")
    Text(",")
    Directive(newline)
    Directive(opt; base)
        Text("levels")
    Text(",")
    Directive(newline)
    Directive(opt; allbase)
        Text("levels")
    Text(",")
    Directive(newline)
    Directive(opt; nofvlabel)
    Text(",")
    Directive(newline)
    Directive(opt; fvwrap(#))
    Text(",")
    Directive(newline)
    Directive(opt; fvwrapon(style))
    Text(",")
    Directive(newline)
    Directive(opth; cformat(%fmt))
    Text(",")
    Directive(newline)
    Directive(opt; pformat(%fmt))
    Text(",")
    Directive(newline)
    Directive(opt; sformat(%fmt))
    Text(", and")
    Directive(newline)
    Directive(opt; nolstretch)
    Text(";")
    Directive(newline)
    Text("    see ")
    Directive(helpb; estimation options##display_options)
        Text("[R] estimation options")
    Text(".")
    Directive(newline)
    Text("    ")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(newline)
    Directive(marker; postestimation)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Postestimation Syntax")
    Directive(newline)
    Directive(newline)
    Text("Only ")
    Directive(cmd)
        Text("estat summarize")
    Text(", ")
    Directive(cmd)
        Text("predict")
    Text(" and ")
    Directive(cmd)
        Text("test")
    Text(" are currently supported and tested.")
    Directive(newline)
    Directive(newline)
    Directive(p; 8 13 2)
    Directive(newline)
    Directive(cmd)
        Text("estat summarize")
    Directive(newline)
    Directive(p_end)
    Directive(col; 23)
    Text("Summarizes ")
    Directive(it)
        Text("depvar")
    Text(" and the variables described in ")
    Directive(it)
        Text("_b")
    Text(" (i.e. not the excluded instruments)")
    Directive(newline)
    Directive(newline)
    Directive(p; 8 16 2)
    Directive(newline)
    Directive(cmd)
        Text("predict")
    Directive(newline)
    Directive(newvar)
    Directive(newline)
    Directive(ifin)
    Directive(newline)
    Text("[")
    Directive(cmd)
        Text(",")
    Text(" ")
    Directive(it)
        Text("statistic")
    Text("]")
    Directive(newline)
    Directive(p_end)
    Directive(col; 23)
    Text("May requires you to previously save the fixed effects (except for option ")
    Directive(opt; xb)
    Text(").")
    Directive(newline)
    Directive(col; 23)
    Text("To see how, see the details of the ")
    Directive(help; reghdfe##absvar)
        Text("absorb")
    Text(" option")
    Directive(newline)
    Directive(col; 23)
    Text("Equation: y = xb + d_absorbvars + e")
    Directive(newline)
    Directive(newline)
    Directive(synoptset; 20 tabbed)
    Directive(nobreak)
    Directive(newline)
    Directive(synopthdr)
        Text("statistic")
    Directive(newline)
    Directive(synoptline)
    Directive(newline)
    Directive(syntab)
        Text("Main")
    Directive(newline)
    Directive(p2coldent)
        Text(" ")
        Directive(opt; xb)
    Text("xb fitted values; the default")
    Directive(p_end)
    Directive(newline)
    Directive(p2coldent)
        Text(" ")
        Directive(opt; xbd)
    Text("xb + d_absorbvars")
    Directive(p_end)
    Directive(newline)
    Directive(p2coldent)
        Text(" ")
        Directive(opt; d)
    Text("d_absorbvars")
    Directive(p_end)
    Directive(newline)
    Directive(p2coldent)
        Text(" ")
        Directive(opt; r)
            Text("esiduals")
    Text("residual")
    Directive(p_end)
    Directive(newline)
    Directive(p2coldent)
        Text(" ")
        Directive(opt; sc)
            Text("ore")
    Text("score; equivalent to ")
    Directive(opt; residuals)
    Directive(p_end)
    Directive(newline)
    Directive(p2coldent)
        Text(" ")
        Directive(opt; stdp)
    Text("standard error of the prediction (of the xb component)")
    Directive(p_end)
    Directive(newline)
    Directive(synoptline)
    Directive(newline)
    Directive(p2colreset)
    Directive(nobreak)
    Directive(newline)
    Directive(p; 4 6 2)
    Text("although ")
    Directive(cmd)
        Text("predict")
    Text(" ")
    Directive(help; data_types)
        Text("type")
    Text(" ")
    Directive(help; newvar)
    Text(" is allowed,")
    Directive(newline)
    Text("the resulting variable will always be of type ")
    Directive(it)
        Text("double")
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(newline)
    Directive(col; 8)
    Directive(cmd)
        Text("test")
    Directive(col; 23)
    Text("Performs significance test on the parameters, see the ")
    Directive(help; test)
        Text("stata help")
    Directive(newline)
    Directive(newline)
    Directive(col; 8)
    Directive(cmd)
        Text("suest")
    Directive(col; 23)
    Text("Do not use ")
    Directive(cmd)
        Text("suest")
    Text(". It will run, but the results will be incorrect. See workaround below")
    Directive(newline)
    Directive(newline)
    Directive(pmore)
    Text("If you want to perform tests that are usually run with ")
    Directive(cmd)
        Text("suest")
    Text(",")
    Directive(newline)
    Text("such as non-nested models, tests using alternative specifications of the variables,")
    Directive(newline)
    Text("or tests on different groups, you can replicate it manually, as described")
    Directive(newline)
    Directive(browse; "http)
        Text("//www.stata.com/statalist/archive/2009-11/msg01485.html":here")
    Text(".")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; remarks)
    Directive(nobreak)
    Directive(newline)
    Directive(newline)
    Directive(title)
        Text("Possible Pitfalls and Common Mistakes")
    Directive(newline)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("1. (note: as of version 2.1, the constant is no longer reported) Ignore the constant; it doesn't tell you much. If you want to use descriptive stats, that's what the ")
    Directive(opt; sum)
        Text("marize()")
    Text(" and ")
    Directive(cmd)
        Text("estat summ")
    Text(" commands are for.")
    Directive(newline)
    Text("Even better, use ")
    Directive(opt; noconstant)
    Text(" to drop it (although it's not really dropped as it never existed on the first place!)")
    Directive(p_end)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("2. Think twice before saving the fixed effects. They are probably inconsistent / not identified and you will likely be using them wrong.")
    Directive(p_end)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("3. (note: as of version 3.0 singletons are dropped by default) It's good practice to drop singletons. ")
    Directive(opt; dropsi)
        Text("ngleton")
    Text(" is your friend.")
    Directive(p_end)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("4. If you use ")
    Directive(opt; vce(robust))
    Text(", be sure that your ")
    Directive(it)
        Text("other")
    Text(" dimension is not "fixed" but grows with N, or your SEs will be wrong.")
    Directive(p_end)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("5. If you use ")
    Directive(opt; vce(cluster ...))
    Text(", check that your number of clusters is high enough (50+ is a rule of thumb). If not, you are making the SEs even worse!")
    Directive(p_end)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("6. The panel variables (absvars) should probably be nested within the clusters (clustervars) due to the within-panel correlation induced by the FEs.")
    Directive(newline)
    Text("(this is not the case for *all* the absvars, only those that are treated as growing as N grows)")
    Directive(p_end)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("7. If you run analytic or probability weights,")
    Directive(newline)
    Text("you are responsible for ensuring that the weights stay")
    Directive(newline)
    Text("constant within each unit of a fixed effect (e.g. individual),")
    Directive(newline)
    Text("or that it is correct to allow varying-weights for that case.")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(p; 5 8 2)
    Text("8. Be aware that adding several HDFEs is not a panacea.")
    Directive(newline)
    Text("The first limitation is that it only uses within variation (more than acceptable if you have a large enough dataset).")
    Directive(newline)
    Text("The second and subtler limitation occurs if the fixed effects are themselves outcomes of the variable of interest (as crazy as it sounds).")
    Directive(newline)
    Text("For instance, imagine a regression where we study the effect of past corporate fraud on future firm performance.")
    Directive(newline)
    Text("We add firm, CEO and time fixed-effects (standard practice). This introduces a serious flaw: whenever a fraud event is discovered,")
    Directive(newline)
    Text("i) future firm performance will suffer, and ii) a CEO turnover will likely occur.")
    Directive(newline)
    Text("Moreover, after fraud events, the new CEOs are usually specialized in dealing with the aftershocks of such events")
    Directive(newline)
    Text("(and are usually accountants or lawyers).")
    Directive(newline)
    Text("The fixed effects of these CEOs will also tend to be quite low, as they tend to manage firms with very risky outcomes.")
    Directive(newline)
    Text("Therefore, the regressor (fraud) affects the fixed effect (identity of the incoming CEO).")
    Directive(newline)
    Text("Adding particularly low CEO fixed effects will then overstate the performance of the firm,")
    Directive(newline)
    Text("and thus ")
    Directive(it)
        Text("understate")
    Text(" the negative effects of fraud on future firm performance.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(title)
        Text("Missing Features")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Text("(If you are interested in discussing these or others, feel free to ")
    Directive(help; reghdfe##contact)
        Text("contact me")
    Text(")")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Text("Code, medium term:")
    Directive(newline)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Complete GT preconditioning (v4)")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Improve algorithm that recovers the fixed effects (v5)")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Improve statistics and tests related to the fixed effects (v5)")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Implement a -bootstrap- option in DoF estimation (v5)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Text("Code, long term:")
    Directive(newline)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("The interaction with cont vars (i.a#c.b) may suffer from numerical accuracy issues, as we are dividing by a sum of squares")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Calculate exact DoF adjustment for 3+ HDFEs (note: not a problem with cluster VCE when one FE is nested within the cluster)")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("More postestimation commands (lincom? margins?)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Text("Theory:")
    Directive(newline)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Add a more thorough discussion on the possible identification issues")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Find out a way to use reghdfe iteratively with CUE")
    Directive(newline)
    Text("(right now only OLS/2SLS/GMM2S/LIML give the exact same results)")
    Directive(p_end)
    Directive(newline)
    Directive(p2col; 5 7 7 2)
        Text(" -")
    Text("Not sure if I should add an F-test for the absvars in the vce(robust) and vce(cluster) cases.")
    Directive(newline)
    Text("Discussion on e.g. -areg- (methods and formulas) and textbooks suggests not;")
    Directive(newline)
    Text("on the other hand, there may be alternatives:")
    Directive(newline)
    Directive(it)
        Directive(browse; "http)
            Text("//www.socialsciences.manchester.ac.uk/disciplines/economics/research/discussionpapers/pdf/EDP-1124.pdf" :A Heteroskedasticity-Robust F-Test Statistic for Individual Effects")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; examples)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Examples")
    Directive(newline)
    Directive(newline)
    Directive(hline)
    Directive(newline)
    Directive(pstd)
    Text("Setup")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". sysuse auto")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Simple case - one fixed effect")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight length, absorb(rep78)")
    Directive(p_end)
    Directive(newline)
    Directive(hline)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("As above, but also compute clustered standard errors")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight length, absorb(rep78) vce(cluster rep78)")
    Directive(p_end)
    Directive(newline)
    Directive(hline)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Two and three sets of fixed effects")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". webuse nlswork")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa south , absorb(idcode year)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa south , absorb(idcode year occ)")
    Directive(p_end)
    Directive(newline)
    Directive(hline)
    Directive(newline)
    Directive(newline)
    Directive(title)
        Text("Advanced examples")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Save the FEs as variables")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa south , absorb(FE1=idcode FE2=year)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Report nested F-tests")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa south , absorb(idcode year) nested")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Do AvgE instead of absorb() for one FE")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa south , absorb(idcode year) avge(occ)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa south , absorb(idcode year) avge(AvgByOCC=occ)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Check that FE coefs are close to 1.0")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa , absorb(idcode year) check")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Save first mobility group")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa , absorb(idcode occ) group(mobility_occ)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Factor interactions in the independent variables")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w i.grade#i.age ttl_exp tenure not_smsa , absorb(idcode occ)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Interactions in the absorbed variables (notice that only the ")
    Directive(it)
        Text("#")
    Text(" symbol is allowed)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp tenure not_smsa , absorb(idcode#occ)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Interactions in both the absorbed and AvgE variables (again, only the ")
    Directive(it)
        Text("#")
    Text(" symbol is allowed)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe ln_w grade age ttl_exp not_smsa , absorb(idcode#occ) avge(tenure#occ)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("IV regression")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". sysuse auto")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight (length=head), absorb(rep78)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight (length=head), absorb(rep78) first")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight (length=head), absorb(rep78) ivsuite(ivregress)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Factorial interactions")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight (length=head), absorb(rep78)")
    Directive(p_end)
    Directive(newline)
    Directive(phang2)
    Directive(cmd)
        Text(". reghdfe price weight length, absorb(rep78 turn##c.price)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(newline)
    Directive(marker; results)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Stored results")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Directive(cmd)
        Text("reghdfe")
    Text(" stores the following in ")
    Directive(cmd)
        Text("e()")
    Text(":")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Directive(it)
        Text("Note: it also keeps most e() results placed by the regression subcommands (ivreg2, ivregress)")
    Directive(newline)
    Directive(newline)
    Directive(synoptset; 24 tabbed)
    Directive(nobreak)
    Directive(newline)
    Directive(syntab)
        Text("Scalars")
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(N)")
    Text("number of observations")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(N_hdfe)")
    Text("number of absorbed fixed-effects")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(tss)")
    Text("total sum of squares")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(rss)")
    Text("residual sum of squares")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(r2)")
    Text("R-squared")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(r2_a)")
    Text("adjusted R-squared")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(r2_within)")
    Text("Within R-squared")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(r2_a_within)")
    Text("Adjusted Within R-squared")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(df_a)")
    Text("degrees of freedom lost due to the fixed effects")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(rmse)")
    Text("root mean squared error")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(ll)")
    Text("log-likelihood")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(ll_0)")
    Text("log-likelihood of fixed-effect-only regression")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(F)")
    Text("F statistic")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(F_absorb)")
    Text("F statistic for absorbed effect ")
    Directive(it)
        Text("note: currently disabled")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(rank)")
    Text("rank of ")
    Directive(cmd)
        Text("e(V)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(N_clustervars)")
    Text("number of cluster variables")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(clust")
        Text("#")
        Directive(cmd)
            Text(")")
    Text("number of clusters for the #th cluster variable")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(N_clust)")
    Text("number of clusters; minimum of ")
    Directive(it)
        Text("e(clust#)")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(K")
        Text("#")
        Directive(cmd)
            Text(")")
    Text("Number of categories of the #th absorbed FE")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(M")
        Text("#")
        Directive(cmd)
            Text(")")
    Text("Number of redundant categories of the #th absorbed FE")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(mobility)")
    Text("Sum of all ")
    Directive(cmd)
        Text("e(M#)")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(df_m)")
    Text("model degrees of freedom")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(df_r)")
    Text("residual degrees of freedom")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(synoptset; 24 tabbed)
    Directive(nobreak)
    Directive(newline)
    Directive(syntab)
        Text("Macros")
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(cmd)")
    Directive(cmd)
        Text("reghdfe")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(subcmd)")
    Text("either ")
    Directive(cmd)
        Text("regress")
    Text(", ")
    Directive(cmd)
        Text("ivreg2")
    Text(" or ")
    Directive(cmd)
        Text("ivregress")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(model)")
    Directive(cmd)
        Text("ols")
    Text(", ")
    Directive(cmd)
        Text("iv")
    Text(", ")
    Directive(cmd)
        Text("gmm2s")
    Text(", ")
    Directive(cmd)
        Text("liml")
    Text(" or ")
    Directive(cmd)
        Text("cue")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(cmdline)")
    Text("command as typed")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(dofmethod)")
    Text("dofmethod employed in the regression")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(depvar)")
    Text("name of dependent variable")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(indepvars)")
    Text("names of independent variables")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(endogvars)")
    Text("names of endogenous right-hand-side variables")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(instruments)")
    Text("names of excluded instruments")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(absvars)")
    Text("name of the absorbed variables or interactions")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(title)")
    Text("title in estimation output")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(clustvar)")
    Text("name of cluster variable")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(clustvar")
        Text("#")
        Directive(cmd)
            Text(")")
    Text("name of the #th cluster variable")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(vce)")
    Directive(it)
        Text("vcetype")
    Text(" specified in ")
    Directive(cmd)
        Text("vce()")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(vcetype)")
    Text("title used to label Std. Err.")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(stage)")
    Text("stage within an IV-regression; only if ")
    Directive(it)
        Text("stages()")
    Text(" was used")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(properties)")
    Directive(cmd)
        Text("b V")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(synoptset; 24 tabbed)
    Directive(nobreak)
    Directive(newline)
    Directive(syntab)
        Text("Matrices")
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(b)")
    Text("coefficient vector")
    Directive(p_end)
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(V)")
    Text("variance-covariance matrix of the estimators")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(synoptset; 24 tabbed)
    Directive(nobreak)
    Directive(newline)
    Directive(syntab)
        Text("Functions")
    Directive(newline)
    Directive(synopt)
        Directive(cmd)
            Text("e(sample)")
    Text("marks estimation sample")
    Directive(p_end)
    Directive(newline)
    Directive(p2colreset)
    Directive(nobreak)
    Directive(newline)
    Directive(newline)
    Directive(marker; contact)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Author")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("Sergio Correia")
    Directive(break)
    Directive(newline)
    Text("Fuqua School of Business, Duke University")
    Directive(break)
    Directive(newline)
    Text("Email: ")
    Directive(browse; "mailto)
        Text("sergio.correia@duke.edu":sergio.correia@duke.edu")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; user_guide)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("User Guide")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Text("A copy of this help file, as well as a more in-depth user guide is in development and will be available at ")
    Directive(browse; "http)
        Text("//scorreia.com/reghdfe"")
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; updates)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Latest Updates")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Directive(cmd)
        Text("reghdfe")
    Text(" is updated frequently, and upgrades or minor bug fixes may not be immediately available in SSC.")
    Directive(newline)
    Text("To check or contribute to the latest version of reghdfe, explore the")
    Directive(newline)
    Directive(browse; "https)
        Text("//github.com/sergiocorreia/reghdfe":Github repository")
    Text(".")
    Directive(newline)
    Text("Bugs or missing features can be discussed through email or at the ")
    Directive(browse; "https)
        Text("//github.com/sergiocorreia/reghdfe/issues":Github issue tracker")
    Text(".")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Text("To see your current version and installed dependencies, type ")
    Directive(cmd)
        Text("reghdfe, version")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; acknowledgements)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("Acknowledgements")
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Directive(newline)
    Text("This package wouldn't have existed without the invaluable feedback and contributions of Paulo Guimaraes,  Amine Ouazad, Mark Schaffer and Kit Baum. Also invaluable are the great bug-spotting abilities of many users.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(pstd)
    Text("In addition, ")
    Directive(it)
        Text("reghdfe")
    Text(" is build upon important contributions from the Stata community:")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(browse; "https)
        Text("//ideas.repec.org/c/boc/bocode/s457101.html":reg2hdfe")
    Text(", from Paulo Guimaraes,")
    Directive(newline)
    Text("and ")
    Directive(browse; "https)
        Text("//ideas.repec.org/c/boc/bocode/s456942.html":a2reg")
    Text(" from Amine Ouazad,")
    Directive(newline)
    Text(" were the inspiration and building blocks on which reghdfe was built.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(browse; "http)
        Text("//www.repec.org/bocode/i/ivreg2.html":ivreg2")
    Text(", by Christopher F Baum, Mark E Schaffer and Steven Stillman, is the package used by default for instrumental-variable regression.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(browse; "https)
        Text("//ideas.repec.org/c/boc/bocode/s457689.html":avar")
    Text(" by Christopher F Baum and Mark E Schaffer, is the package used for estimating the HAC-robust standard errors of ols regressions.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(browse; "http)
        Text("//econpapers.repec.org/software/bocbocode/s456797.htm)":tuples")
    Text(" by Joseph Lunchman and Nicholas Cox, is used when computing standard errors with multi-way clustering (two or more clustering variables).")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(marker; references)
    Directive(nobreak)
    Directive(newline)
    Directive(title)
        Text("References")
    Directive(newline)
    Directive(newline)
    Directive(p; 0 0 2)
    Directive(newline)
    Text("The algorithm underlying reghdfe is a generalization of the works by:")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Text("Paulo Guimaraes and Pedro Portugal. "A Simple Feasible Alternative Procedure to Estimate")
    Directive(newline)
    Text("Models with High-Dimensional Fixed Effects".")
    Directive(newline)
    Directive(it)
        Text("Stata Journal, 10(4), 628-649, 2010.")
    Directive(newline)
    Directive(browse; "http)
        Text("//www.stata-journal.com/article.html?article=st0212":[link]")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Text("Simen Gaure. "OLS with Multiple High Dimensional Category Dummies".")
    Directive(newline)
    Directive(it)
        Text("Memorandum 14/2010, Oslo University, Department of Economics, 2010.")
    Directive(newline)
    Directive(browse; "https)
        Text("//ideas.repec.org/p/hhs/osloec/2010_014.html":[link]")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(p; 0 0 2)
    Directive(newline)
    Text("It addresses many of the limitation of previous works, such as possible lack of convergence, arbitrary slow convergence times,")
    Directive(newline)
    Text("and being limited to only two or three sets of fixed effects (for the first paper).")
    Directive(newline)
    Text("The paper explaining the specifics of the algorithm is a work-in-progress and available upon request.")
    Directive(newline)
    Directive(newline)
    Directive(p; 0 0 0)
    Directive(newline)
    Text("If you use this program in your research, please cite either")
    Directive(newline)
    Text("the ")
    Directive(browse; "https)
        Text("//ideas.repec.org/c/boc/bocode/s457874.html":REPEC entry")
    Directive(newline)
    Text("or the aforementioned papers.")
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(title)
        Text("Additional References")
    Directive(newline)
    Directive(newline)
    Directive(p; 0 0 0)
    Directive(newline)
    Text("For details on the Aitken acceleration technique employed, please see "method 3" as described by:")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Text("Macleod, Allan J. "Acceleration of vector sequences by multi-dimensional Delta-2 methods."")
    Directive(newline)
    Directive(it)
        Text("Communications in Applied Numerical Methods 2.4 (1986): 385-392.")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(p; 0 0 0)
    Directive(newline)
    Text("For the rationale behind interacting fixed effects with continuous variables, see:")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Text("Duflo, Esther. "The medium run effects of educational expansion: Evidence from a large school construction program in Indonesia."")
    Directive(newline)
    Directive(it)
        Text("Journal of Development Economics 74.1 (2004): 163-197.")
    Directive(browse; "http)
        Text("//www.sciencedirect.com/science/article/pii/S0304387803001846": [link]")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(p; 0 0 0)
    Directive(newline)
    Text("Also see:")
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Text("Abowd, J. M., R. H. Creecy, and F. Kramarz 2002.")
    Directive(newline)
    Text("Computing person and firm effects using linked longitudinal employer-employee data.")
    Directive(newline)
    Directive(it)
        Text("Census Bureau Technical Paper TP-2002-06.")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Text("Cameron, A. Colin & Gelbach, Jonah B. & Miller, Douglas L., 2011.")
    Directive(newline)
    Text(""Robust Inference With Multiway Clustering,"")
    Directive(newline)
    Directive(it)
        Text("Journal of Business & Economic Statistics, American Statistical Association, vol. 29(2), pages 238-249.")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Text("Gormley, T. & Matsa, D. 2014.")
    Directive(newline)
    Text(""Common errors: How to (and not to) control for unobserved heterogeneity."")
    Directive(newline)
    Directive(it)
        Text("The Review of Financial Studies, vol. 27(2), pages 617-661.")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
    Directive(newline)
    Directive(phang)
    Directive(newline)
    Text("Mittag, N. 2012.")
    Directive(newline)
    Text(""New methods to estimate models with large sets of fixed effects with an application to matched employer-employee data from Germany."")
    Directive(newline)
    Directive(it)
        Directive(browse; "http)
            Text("//doku.iab.de/fdz/reporte/2012/MR_01-12_EN.pdf":FDZ-Methodenreport 02/2012")
        Text(".")
    Directive(newline)
    Directive(p_end)
    Directive(newline)
