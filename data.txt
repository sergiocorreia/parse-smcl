NOT USED: Directive(p2colreset) []
NOT USED: Directive(nobreak) []
NOT USED: Directive(p2colreset) []
NOT USED: Directive(nobreak) []
NOT USED: Directive(p2colreset) []
NOT USED: Directive(nobreak) []
NOT USED: Directive(p2colreset) []
NOT USED: Directive(nobreak) []
REMAINING: 1572
SMCL('jumpto': [['Syntax', 'reghdfe##syntax'], ['Description', 'reghdfe##description'], ['Options', 'reghdfe##options'], ['Postestimation Syntax', 'reghdfe##postestimation'], ['Remarks', 'reghdfe##remarks'], ['Examples', 'reghdfe##examples'], ['Stored results', 'reghdfe##results'], ['Author', 'reghdfe##contact'], ['Updates', 'reghdfe##updates'], ['Acknowledgements', 'reghdfe##acknowledgements'], ['References', 'reghdfe##references']], 'version': ['VERSION_NUMBER'], 'alsosee': [['[R] areg', 'help areg'], ['[R] xtreg', 'help xtreg'], ['[R] ivregress', 'help ivregress'], ['', '--'], ['ivreg2', 'help ivreg2'], ['ivregress', 'help ivregress'], ['reg2hdfe', 'help reg2hdfe'], ['a2reg', 'help a2reg']])
    Heading('level': 1)
        TextDirective("Title")
    Table('margins': [])
        TableRow()
            TableData()
                Directive(cmd)
                    TextDirective("reghdfe")
                TextDirective(" ")
                Directive(hline; 2)
            TableData()
                TextDirective("Linear and instrumental-variable/GMM regression absorbing multiple levels of fixed effects")
                Directive(p2colreset)
                Directive(nobreak)
                Directive(newline)
    Heading('level': 1, 'id': 'syntax')
        TextDirective("Syntax")
    Para('margins': [8, 15, 2])
        TextDirective(" ")
        Directive(cmd)
            TextDirective("reghdfe")
        Directive(depvar)
        TextDirective(" [")
        Directive(indepvars)
        TextDirective("] [")
        Directive(cmd)
            TextDirective("(")
        Directive(it)
            Directive(help; varlist)
                TextDirective("endogvars")
        TextDirective(" ")
        Directive(cmd)
            TextDirective("=")
        TextDirective(" ")
        Directive(it)
            Directive(help; varlist)
                TextDirective("iv_vars")
        Directive(cmd)
            TextDirective(")")
        TextDirective("]")
        Directive(ifin)
        TextDirective(" ")
        Directive(it)
            Directive(weight)
        TextDirective(" ")
        Directive(cmd)
            TextDirective(",")
        TextDirective(" ")
        Directive(opth; a)
            TextDirective("bsorb(reghdfe##absvar:absvars)")
        TextDirective(" [")
        Directive(help; reghdfe##options)
            TextDirective("options")
        TextDirective("] ")
    SyntaxTable('id': 'opt_summary')
        TableHead()
            TableRow()
                TableData('colspan': 2)
                    Text("options")
                TableData()
                    Text("Description")
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Model ")
                    Directive(help; reghdfe##opt_model)
                        TextDirective("[+]")
            TableRow('style': 'has_footnote')
                TableData()
                    Text("*")
                TableData()
                    Directive(opth; a)
                        TextDirective("bsorb(reghdfe##absvar:absvars)")
                TableData()
                    TextDirective("identifiers of the absorbed fixed effects; each ")
                    Directive(help; reghdfe##absvar)
                        TextDirective("absvar")
                    TextDirective(" represents one set of fixed effects")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    TextDirective(" ")
                    Directive(cmdab)
                        TextDirective("a:bsorb(")
                    Directive(it)
                        TextDirective("...")
                    Directive(cmd)
                        TextDirective(",")
                    TextDirective(" ")
                    Directive(cmdab)
                        TextDirective("save:fe)")
                TableData()
                    TextDirective("save all fixed effect estimates (")
                    Directive(it)
                        TextDirective("__hdfe*")
                    TextDirective(" prefix); useful for a subsequent ")
                    Directive(help; reghdfe##postestimation)
                        TextDirective("predict")
                    TextDirective(".")
                    Directive(newline)
                    TextDirective("However, see also the ")
                    Directive(it)
                        TextDirective("resid")
                    TextDirective(" option.")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    TextDirective(" ")
                    Directive(opth; res)
                        TextDirective("iduals(newvar)")
                TableData()
                    TextDirective("save residuals; more direct and much faster than saving the fixed effects and then running predict")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opth; su)
                        TextDirective("mmarize(tabstat##statname:stats)")
                TableData()
                    TextDirective("equivalent to ")
                    Directive(help; reghdfe##postestimation)
                        TextDirective("estat summarize")
                    TextDirective(" after the regression,")
                    Directive(newline)
                    TextDirective("but more flexible, compatible with the ")
                    Directive(opt; fast)
                    TextDirective(" option, and saves results on ")
                    Directive(it)
                        TextDirective("e(summarize)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    TextDirective(" ")
                    Directive(opt; subopt)
                        TextDirective("ions(...)")
                TableData()
                    TextDirective("additional options that will be passed to the regression command (either ")
                    Directive(help; regress)
                    TextDirective(", ")
                    Directive(help; ivreg2)
                    TextDirective(", or ")
                    Directive(help; ivregress)
                    TextDirective(")")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("SE/Robust ")
                    Directive(help; reghdfe##opt_vce)
                        TextDirective("[+]")
            TableRow('style': 'has_footnote')
                TableData()
                    Text("+")
                TableData()
                    Directive(opt; vce)
                    Directive(cmd)
                        TextDirective("(")
                    Directive(help; reghdfe##opt_vce)
                        TextDirective("vcetype")
                    TextDirective(" [")
                    Directive(cmd)
                        TextDirective(",")
                    Directive(it)
                        TextDirective("opt")
                    TextDirective("]")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    Directive(it)
                        TextDirective("vcetype")
                    Directive(newline)
                    TextDirective("may be ")
                    Directive(opt; un)
                        TextDirective("adjusted")
                    TextDirective(" (default), ")
                    Directive(opt; r)
                        TextDirective("obust")
                    TextDirective(" or ")
                    Directive(opt; cl)
                        TextDirective("uster")
                    TextDirective(" ")
                    Directive(help; fvvarlist)
                    TextDirective(" (allowing two- and multi-way clustering)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                TableData()
                    TextDirective("suboptions ")
                    Directive(opt; bw(#))
                    TextDirective(", ")
                    Directive(opt; ker)
                        TextDirective("nel(str)")
                    TextDirective(", ")
                    Directive(opt; dkraay(#))
                    TextDirective(" and ")
                    Directive(opt; kiefer)
                    TextDirective(" allow for AC/HAC estimates; see the ")
                    Directive(help; avar)
                    TextDirective(" package")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Instrumental-Variable/2SLS/GMM ")
                    Directive(help; reghdfe##opt_iv)
                        TextDirective("[+]")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; est)
                        TextDirective("imator(str)")
                TableData()
                    TextDirective("either ")
                    Directive(opt; 2sls)
                    TextDirective(" (default), ")
                    Directive(opt; gmm)
                        TextDirective("2s")
                    TextDirective(" (two-stage GMM),")
                    Directive(newline)
                    Directive(opt; liml)
                    TextDirective(" (limited-information maximum likelihood) or ")
                    Directive(opt; cue)
                    TextDirective(" (which gives approximate results, see discussion below)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; stage)
                        TextDirective("s(list)")
                TableData()
                    TextDirective("estimate additional regressions; choose any of ")
                    Directive(opt; first)
                    TextDirective(" ")
                    Directive(opt; ols)
                    TextDirective(" ")
                    Directive(opt; reduced)
                    TextDirective(" ")
                    Directive(opt; acid)
                    TextDirective(" (or ")
                    Directive(opt; all)
                    TextDirective(")")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; ff)
                        TextDirective("irst")
                TableData()
                    TextDirective("compute first-stage diagnostic and identification statistics")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opth; iv)
                        TextDirective("suite(subcmd)")
                TableData()
                    TextDirective("package used in the IV/GMM regressions;")
                    Directive(newline)
                    TextDirective("options are ")
                    Directive(opt; ivreg2)
                    TextDirective(" (default; needs installing) and ")
                    Directive(opt; ivregress)
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Diagnostic ")
                    Directive(help; reghdfe##opt_diagnostic)
                        TextDirective("[+]")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; v)
                        TextDirective("erbose(#)")
                TableData()
                    TextDirective("amount of debugging information to show (0=None, 1=Some, 2=More, 3=Parsing/convergence details, 4=Every iteration)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; time)
                        TextDirective("it")
                TableData()
                    TextDirective("show elapsed times by stage of computation")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Optimization ")
                    Directive(help; reghdfe##opt_optimization)
                        TextDirective("[+]")
            TableRow('style': 'has_footnote')
                TableData()
                    Text("+")
                TableData()
                    Directive(opth; tol)
                        TextDirective("erance(#)")
                TableData()
                    TextDirective("criterion for convergence (default=1e-8)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opth; maxit)
                        TextDirective("erations(#)")
                TableData()
                    TextDirective("maximum number of iterations (default=10,000); if set to missing (")
                    Directive(cmd)
                        TextDirective(".")
                    TextDirective(") it will run for as long as it takes.")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opth; pool)
                        TextDirective("size(#)")
                TableData()
                    TextDirective("apply the within algorithm in groups of ")
                    Directive(it)
                        TextDirective("#")
                    TextDirective(" variables (default 10). a large poolsize is usually faster but uses more memory")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; accel)
                        TextDirective("eration(str)")
                TableData()
                    TextDirective("acceleration method; options are conjugate_gradient (cg), steep_descent (sd), aitken (a), and none (no)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; transf)
                        TextDirective("orm(str)")
                TableData()
                    TextDirective("transform operation that defines the type of alternating projection; options are Kaczmarz (kac), Cimmino (cim), Symmetric Kaczmarz (sym)")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Speedup Tricks ")
                    Directive(help; reghdfe##opt_speedup)
                        TextDirective("[+]")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective(" cache(save")
                    TextDirective(" [,opt]")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    TextDirective("absorb all variables without regressing (destructive; combine it with ")
                    Directive(help; preserve)
                        TextDirective("preserve/restore")
                    TextDirective(")")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                TableData()
                    TextDirective("suboption ")
                    Directive(opth; keep(varlist))
                    TextDirective(" adds additional untransformed variables to the resulting dataset")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective(" cache(use)")
                TableData()
                    TextDirective("run regressions on cached data; ")
                    Directive(it)
                        TextDirective("vce()")
                    TextDirective(" must be the same as with ")
                    Directive(cmd)
                        TextDirective(" cache(save)")
                    TextDirective(".")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective(" cache(clear)")
                TableData()
                    TextDirective("delete Mata objects to clear up memory; no more regressions can be run after this")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; fast)
                TableData()
                    TextDirective("will not create ")
                    Directive(it)
                        TextDirective("e(sample)")
                    TextDirective("; disabled when saving fixed effects, residuals or mobility groups")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Degrees-of-Freedom Adjustments ")
                    Directive(help; reghdfe##opt_dof)
                        TextDirective("[+]")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; dof)
                        TextDirective("adjustments(list)")
                TableData()
                    TextDirective("allows selecting the desired adjustments for degrees of freedom;")
                    Directive(newline)
                    TextDirective("rarely used")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    TextDirective(" ")
                    Directive(opth; groupv)
                        TextDirective("ar(newvar)")
                TableData()
                    TextDirective("unique identifier for the first mobility group")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Reporting ")
                    Directive(help; reghdfe##opt_reporting)
                        TextDirective("[+]")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; version)
                TableData()
                    TextDirective("reports the version number and date of reghdfe, and saves it in e(version). standalone option")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; l)
                        TextDirective("evel(#)")
                TableData()
                    TextDirective("set confidence level; default is ")
                    Directive(cmd)
                        TextDirective("level(95)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(it)
                        Directive(help; reghdfe##display_options)
                            TextDirective("display_options")
                TableData()
                    TextDirective("control column formats, row spacing, line width, display of omitted variables and base and empty cells, and factor-variable labeling.")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                TableData()
                    TextDirective("particularly useful are the ")
                    Directive(opt; noomit)
                        TextDirective("ted")
                    TextDirective(" and ")
                    Directive(opt; noempty)
                    TextDirective(" options to hide regressors omitted due to collinearity")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Undocumented")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; keepsin)
                        TextDirective("gletons")
                TableData()
                    TextDirective("do not drop singleton groups")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(opt; old)
                TableData()
                    TextDirective("will call the latest 2.x version of reghdfe instead (see the ")
                    Directive(help; reghdfe_old)
                        TextDirective("old help file")
                    TextDirective(")")
        TableFoot()
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("* ")
                    Directive(opt; absorb(absvars))
                    TextDirective(" is required.")
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("+ indicates a recommended or important option.")
            TableRow('style': 'footnote')
                TableData()
                    Directive(it)
                        TextDirective("indepvars")
                    TextDirective(", ")
                    Directive(it)
                        TextDirective("endogvars")
                    TextDirective(" and ")
                    Directive(it)
                        TextDirective("iv_vars")
                    TextDirective(" may contain factor variables; see ")
                    Directive(help; fvvarlist)
                    TextDirective(".")
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("all the regression variables may contain time-series operators; see ")
                    Directive(help; tsvarlist)
                    TextDirective(".")
            TableRow('style': 'footnote')
                TableData()
                    Directive(cmd)
                        TextDirective("fweight")
                    TextDirective("s, ")
                    Directive(cmd)
                        TextDirective("aweight")
                    TextDirective("s and ")
                    Directive(cmd)
                        TextDirective("pweight")
                    TextDirective("s are allowed; see ")
                    Directive(help; weight)
                    TextDirective(".")
    Heading('level': 1, 'id': 'absvar')
        TextDirective("Absvar Syntax")
    SyntaxTable()
        TableHead()
            TableRow()
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("absvar")
                TableData()
                    Text("Description")
        TableBody()
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("i.")
                    Directive(it)
                        TextDirective("varname")
                TableData()
                    TextDirective("categorical variable to be absorbed (the ")
                    Directive(cmd)
                        TextDirective("i.")
                    TextDirective(" prefix is tacit)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("i.")
                    Directive(it)
                        TextDirective("var1")
                    Directive(cmd)
                        TextDirective("#i.")
                    Directive(it)
                        TextDirective("var2")
                TableData()
                    TextDirective("absorb the interactions of multiple categorical variables")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("i.")
                    Directive(it)
                        TextDirective("var1")
                    Directive(cmd)
                        TextDirective("#")
                    Directive(cmd)
                        TextDirective("c.")
                    Directive(it)
                        TextDirective("var2")
                TableData()
                    TextDirective("absorb heterogeneous slopes, where ")
                    Directive(it)
                        TextDirective("var2")
                    TextDirective(" has a different slope coef. depending on the category of ")
                    Directive(it)
                        TextDirective("var1")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(it)
                        TextDirective("var1")
                    Directive(cmd)
                        TextDirective("##")
                    Directive(cmd)
                        TextDirective("c.")
                    Directive(it)
                        TextDirective("var2")
                TableData()
                    TextDirective("equivalent to "")
                    Directive(cmd)
                        TextDirective("i.")
                    Directive(it)
                        TextDirective("var1")
                    TextDirective(" ")
                    Directive(cmd)
                        TextDirective("i.")
                    Directive(it)
                        TextDirective("var1")
                    Directive(cmd)
                        TextDirective("#")
                    Directive(cmd)
                        TextDirective("c.")
                    Directive(it)
                        TextDirective("var2")
                    TextDirective("", but ")
                    Directive(it)
                        TextDirective("much")
                    TextDirective(" faster")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(it)
                        TextDirective("var1")
                    Directive(cmd)
                        TextDirective("##c.(")
                    Directive(it)
                        TextDirective("var2 var3")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    TextDirective("multiple heterogeneous slopes are allowed together. Alternative syntax: ")
                    Directive(it)
                        TextDirective("var1")
                    Directive(cmd)
                        TextDirective("##(c.")
                    Directive(it)
                        TextDirective("var2")
                    TextDirective(" ")
                    Directive(cmd)
                        TextDirective("c.")
                    Directive(it)
                        TextDirective("var3")
                    Directive(cmd)
                        TextDirective(")")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(it)
                        TextDirective("v1")
                    Directive(cmd)
                        TextDirective("#")
                    Directive(it)
                        TextDirective("v2")
                    Directive(cmd)
                        TextDirective("#")
                    Directive(it)
                        TextDirective("v3")
                    Directive(cmd)
                        TextDirective("##c.(")
                    Directive(it)
                        TextDirective("v4 v5")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    TextDirective("factor operators can be combined")
        TableFoot()
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("To save the estimates specific absvars, write ")
                    Directive(newvar)
                    Directive(inp)
                        TextDirective("=")
                        Directive(it)
                            TextDirective("absvar")
                    TextDirective(".")
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("Please be aware that in most cases these estimates are neither consistent nor econometrically identified.")
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("Using categorical interactions (e.g. ")
                    Directive(it)
                        TextDirective("x")
                    Directive(cmd)
                        TextDirective("#")
                    Directive(it)
                        TextDirective("z")
                    TextDirective(") is faster than running ")
                    Directive(it)
                        TextDirective("egen group(...)")
                    TextDirective(" beforehand.")
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("Singleton obs. are dropped iteratively until no more singletons are found (see ancilliary article for details).")
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("Slope-only absvars ("state#c.time") have poor numerical stability and slow convergence.")
                    TextDirective("If you need those, either i) increase tolerance or")
                    TextDirective("ii) use slope-and-intercept absvars ("state##c.time"), even if the intercept is redundant.")
                    TextDirective("For instance if absvar is "i.zipcode i.state##c.time" then i.state is redundant given i.zipcode, but")
                    TextDirective("convergence will still be ")
                    Directive(it)
                        TextDirective("much")
                    TextDirective(" faster.")
    Heading('level': 1, 'id': 'description')
        TextDirective("Description")
    Table('margins': [6, 9, 9, 2])
        TableRow()
            TableData()
                TextDirective(" a)")
            TableData()
                TextDirective("A novel and robust algorithm to efficiently absorb the fixed effects (extending the work of Guimaraes and Portugal, 2010).")
        TableRow()
            TableData()
                TextDirective(" b)")
            TableData()
                TextDirective("Coded in Mata, which in most scenarios makes it even faster than ")
                Directive(it)
                    TextDirective("areg")
                TextDirective(" and ")
                Directive(it)
                    TextDirective("xtreg")
                TextDirective(" for a single fixed effect (see benchmarks on the Github page).")
        TableRow()
            TableData()
                TextDirective(" c)")
            TableData()
                TextDirective("Can save the point estimates of the fixed effects (")
                Directive(it)
                    TextDirective("caveat emptor")
                TextDirective(": the fixed effects may not be identified, see the ")
                Directive(help; reghdfe##references)
                    TextDirective("references")
                TextDirective(").")
        TableRow()
            TableData()
                TextDirective(" d)")
            TableData()
                TextDirective("Calculates the degrees-of-freedom lost due to the fixed effects")
                Directive(newline)
                TextDirective("(note: beyond two levels of fixed effects, this is still an open problem, but we provide a conservative approximation).")
        TableRow()
            TableData()
                TextDirective(" e)")
            TableData()
                TextDirective("Iteratively removes singleton groups by default, to avoid biasing the standard errors (see ancillary document).")
    Heading('level': 1, 'id': 'options')
        TextDirective("Options")
    Heading('level': 1, 'id': 'postestimation')
        TextDirective("Postestimation Syntax")
    Para('margins': [8, 13, 2])
        Directive(cmd)
            TextDirective("estat summarize")
    Para('margins': [8, 16, 2])
        Directive(cmd)
            TextDirective("predict")
        Directive(newvar)
        Directive(ifin)
        TextDirective("[")
        Directive(cmd)
            TextDirective(",")
        TextDirective(" ")
        Directive(it)
            TextDirective("statistic")
        TextDirective("]")
    SyntaxTable()
        TableHead()
            TableRow()
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("statistic")
                TableData()
                    Text("Description")
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Main")
            TableRow('style': 'has_footnote')
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("statistic")
                TableData('colspan': 2)
                    TextDirective(" ")
                    Directive(opt; xb)
                TableData()
                    TextDirective("xb fitted values; the default")
            TableRow('style': 'has_footnote')
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("statistic")
                TableData('colspan': 2)
                    TextDirective(" ")
                    Directive(opt; xbd)
                TableData()
                    TextDirective("xb + d_absorbvars")
            TableRow('style': 'has_footnote')
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("statistic")
                TableData('colspan': 2)
                    TextDirective(" ")
                    Directive(opt; d)
                TableData()
                    TextDirective("d_absorbvars")
            TableRow('style': 'has_footnote')
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("statistic")
                TableData('colspan': 2)
                    TextDirective(" ")
                    Directive(opt; r)
                        TextDirective("esiduals")
                TableData()
                    TextDirective("residual")
            TableRow('style': 'has_footnote')
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("statistic")
                TableData('colspan': 2)
                    TextDirective(" ")
                    Directive(opt; sc)
                        TextDirective("ore")
                TableData()
                    TextDirective("score; equivalent to ")
                    Directive(opt; residuals)
            TableRow('style': 'has_footnote')
                TableData('colspan': 2)
                    Directive(synopthdr)
                        TextDirective("statistic")
                TableData('colspan': 2)
                    TextDirective(" ")
                    Directive(opt; stdp)
                TableData()
                    TextDirective("standard error of the prediction (of the xb component)")
        TableFoot()
            TableRow('style': 'footnote')
                TableData()
                    TextDirective("although ")
                    Directive(cmd)
                        TextDirective("predict")
                    TextDirective(" ")
                    Directive(help; data_types)
                        TextDirective("type")
                    TextDirective(" ")
                    Directive(help; newvar)
                    TextDirective(" is allowed,")
                    TextDirective("the resulting variable will always be of type ")
                    Directive(it)
                        TextDirective("double")
                    TextDirective(".")
    Heading('level': 1, 'id': 'remarks')
        TextDirective("Possible Pitfalls and Common Mistakes")
    Para('margins': [5, 8, 2])
        TextDirective("1. (note: as of version 2.1, the constant is no longer reported) Ignore the constant; it doesn't tell you much. If you want to use descriptive stats, that's what the ")
        Directive(opt; sum)
            TextDirective("marize()")
        TextDirective(" and ")
        Directive(cmd)
            TextDirective("estat summ")
        TextDirective(" commands are for.")
        TextDirective("Even better, use ")
        Directive(opt; noconstant)
        TextDirective(" to drop it (although it's not really dropped as it never existed on the first place!)")
    Para('margins': [5, 8, 2])
        TextDirective("2. Think twice before saving the fixed effects. They are probably inconsistent / not identified and you will likely be using them wrong.")
    Para('margins': [5, 8, 2])
        TextDirective("3. (note: as of version 3.0 singletons are dropped by default) It's good practice to drop singletons. ")
        Directive(opt; dropsi)
            TextDirective("ngleton")
        TextDirective(" is your friend.")
    Para('margins': [5, 8, 2])
        TextDirective("4. If you use ")
        Directive(opt; vce(robust))
        TextDirective(", be sure that your ")
        Directive(it)
            TextDirective("other")
        TextDirective(" dimension is not "fixed" but grows with N, or your SEs will be wrong.")
    Para('margins': [5, 8, 2])
        TextDirective("5. If you use ")
        Directive(opt; vce(cluster ...))
        TextDirective(", check that your number of clusters is high enough (50+ is a rule of thumb). If not, you are making the SEs even worse!")
    Para('margins': [5, 8, 2])
        TextDirective("6. The panel variables (absvars) should probably be nested within the clusters (clustervars) due to the within-panel correlation induced by the FEs.")
        TextDirective("(this is not the case for *all* the absvars, only those that are treated as growing as N grows)")
    Para('margins': [5, 8, 2])
        TextDirective("7. If you run analytic or probability weights,")
        TextDirective("you are responsible for ensuring that the weights stay")
        TextDirective("constant within each unit of a fixed effect (e.g. individual),")
        TextDirective("or that it is correct to allow varying-weights for that case.")
    Para('margins': [5, 8, 2])
        TextDirective("8. Be aware that adding several HDFEs is not a panacea.")
        TextDirective("The first limitation is that it only uses within variation (more than acceptable if you have a large enough dataset).")
        TextDirective("The second and subtler limitation occurs if the fixed effects are themselves outcomes of the variable of interest (as crazy as it sounds).")
        TextDirective("For instance, imagine a regression where we study the effect of past corporate fraud on future firm performance.")
        TextDirective("We add firm, CEO and time fixed-effects (standard practice). This introduces a serious flaw: whenever a fraud event is discovered,")
        TextDirective("i) future firm performance will suffer, and ii) a CEO turnover will likely occur.")
        TextDirective("Moreover, after fraud events, the new CEOs are usually specialized in dealing with the aftershocks of such events")
        TextDirective("(and are usually accountants or lawyers).")
        TextDirective("The fixed effects of these CEOs will also tend to be quite low, as they tend to manage firms with very risky outcomes.")
        TextDirective("Therefore, the regressor (fraud) affects the fixed effect (identity of the incoming CEO).")
        TextDirective("Adding particularly low CEO fixed effects will then overstate the performance of the firm,")
        TextDirective("and thus ")
        Directive(it)
            TextDirective("understate")
        TextDirective(" the negative effects of fraud on future firm performance.")
    Heading('level': 1)
        TextDirective("Missing Features")
    Table('margins': [5, 7, 7, 2])
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Complete GT preconditioning (v4)")
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Improve algorithm that recovers the fixed effects (v5)")
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Improve statistics and tests related to the fixed effects (v5)")
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Implement a -bootstrap- option in DoF estimation (v5)")
    Table('margins': [5, 7, 7, 2])
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("The interaction with cont vars (i.a#c.b) may suffer from numerical accuracy issues, as we are dividing by a sum of squares")
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Calculate exact DoF adjustment for 3+ HDFEs (note: not a problem with cluster VCE when one FE is nested within the cluster)")
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("More postestimation commands (lincom? margins?)")
    Table('margins': [5, 7, 7, 2])
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Add a more thorough discussion on the possible identification issues")
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Find out a way to use reghdfe iteratively with CUE")
                Directive(newline)
                TextDirective("(right now only OLS/2SLS/GMM2S/LIML give the exact same results)")
        TableRow()
            TableData()
                TextDirective(" -")
            TableData()
                TextDirective("Not sure if I should add an F-test for the absvars in the vce(robust) and vce(cluster) cases.")
                Directive(newline)
                TextDirective("Discussion on e.g. -areg- (methods and formulas) and textbooks suggests not;")
                Directive(newline)
                TextDirective("on the other hand, there may be alternatives:")
                Directive(newline)
                Directive(it)
                    Directive(browse; "http)
                        TextDirective("//www.socialsciences.manchester.ac.uk/disciplines/economics/research/discussionpapers/pdf/EDP-1124.pdf" :A Heteroskedasticity-Robust F-Test Statistic for Individual Effects")
    Heading('level': 1, 'id': 'examples')
        TextDirective("Examples")
    Heading('level': 1)
        TextDirective("Advanced examples")
    Heading('level': 1, 'id': 'results')
        TextDirective("Stored results")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Scalars")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(N)")
                TableData()
                    TextDirective("number of observations")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(N_hdfe)")
                TableData()
                    TextDirective("number of absorbed fixed-effects")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(tss)")
                TableData()
                    TextDirective("total sum of squares")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(rss)")
                TableData()
                    TextDirective("residual sum of squares")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(r2)")
                TableData()
                    TextDirective("R-squared")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(r2_a)")
                TableData()
                    TextDirective("adjusted R-squared")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(r2_within)")
                TableData()
                    TextDirective("Within R-squared")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(r2_a_within)")
                TableData()
                    TextDirective("Adjusted Within R-squared")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(df_a)")
                TableData()
                    TextDirective("degrees of freedom lost due to the fixed effects")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(rmse)")
                TableData()
                    TextDirective("root mean squared error")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(ll)")
                TableData()
                    TextDirective("log-likelihood")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(ll_0)")
                TableData()
                    TextDirective("log-likelihood of fixed-effect-only regression")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(F)")
                TableData()
                    TextDirective("F statistic")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(F_absorb)")
                TableData()
                    TextDirective("F statistic for absorbed effect ")
                    Directive(it)
                        TextDirective("note: currently disabled")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(rank)")
                TableData()
                    TextDirective("rank of ")
                    Directive(cmd)
                        TextDirective("e(V)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(N_clustervars)")
                TableData()
                    TextDirective("number of cluster variables")
    SyntaxTable()
        TableBody()
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(clust")
                    TextDirective("#")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    TextDirective("number of clusters for the #th cluster variable")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(N_clust)")
                TableData()
                    TextDirective("number of clusters; minimum of ")
                    Directive(it)
                        TextDirective("e(clust#)")
    SyntaxTable()
        TableBody()
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(K")
                    TextDirective("#")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    TextDirective("Number of categories of the #th absorbed FE")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(M")
                    TextDirective("#")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    TextDirective("Number of redundant categories of the #th absorbed FE")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(mobility)")
                TableData()
                    TextDirective("Sum of all ")
                    Directive(cmd)
                        TextDirective("e(M#)")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(df_m)")
                TableData()
                    TextDirective("model degrees of freedom")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(df_r)")
                TableData()
                    TextDirective("residual degrees of freedom")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Macros")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(cmd)")
                TableData()
                    Directive(cmd)
                        TextDirective("reghdfe")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(subcmd)")
                TableData()
                    TextDirective("either ")
                    Directive(cmd)
                        TextDirective("regress")
                    TextDirective(", ")
                    Directive(cmd)
                        TextDirective("ivreg2")
                    TextDirective(" or ")
                    Directive(cmd)
                        TextDirective("ivregress")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(model)")
                TableData()
                    Directive(cmd)
                        TextDirective("ols")
                    TextDirective(", ")
                    Directive(cmd)
                        TextDirective("iv")
                    TextDirective(", ")
                    Directive(cmd)
                        TextDirective("gmm2s")
                    TextDirective(", ")
                    Directive(cmd)
                        TextDirective("liml")
                    TextDirective(" or ")
                    Directive(cmd)
                        TextDirective("cue")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(cmdline)")
                TableData()
                    TextDirective("command as typed")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(dofmethod)")
                TableData()
                    TextDirective("dofmethod employed in the regression")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(depvar)")
                TableData()
                    TextDirective("name of dependent variable")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(indepvars)")
                TableData()
                    TextDirective("names of independent variables")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(endogvars)")
                TableData()
                    TextDirective("names of endogenous right-hand-side variables")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(instruments)")
                TableData()
                    TextDirective("names of excluded instruments")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(absvars)")
                TableData()
                    TextDirective("name of the absorbed variables or interactions")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(title)")
                TableData()
                    TextDirective("title in estimation output")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(clustvar)")
                TableData()
                    TextDirective("name of cluster variable")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(clustvar")
                    TextDirective("#")
                    Directive(cmd)
                        TextDirective(")")
                TableData()
                    TextDirective("name of the #th cluster variable")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(vce)")
                TableData()
                    Directive(it)
                        TextDirective("vcetype")
                    TextDirective(" specified in ")
                    Directive(cmd)
                        TextDirective("vce()")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(vcetype)")
                TableData()
                    TextDirective("title used to label Std. Err.")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(stage)")
                TableData()
                    TextDirective("stage within an IV-regression; only if ")
                    Directive(it)
                        TextDirective("stages()")
                    TextDirective(" was used")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(properties)")
                TableData()
                    Directive(cmd)
                        TextDirective("b V")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Matrices")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(b)")
                TableData()
                    TextDirective("coefficient vector")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(V)")
                TableData()
                    TextDirective("variance-covariance matrix of the estimators")
    SyntaxTable()
        TableBody()
            TableRow('style': 'section')
                TableData('colspan': 3)
                    TextDirective("Functions")
            TableRow('style': 'normal')
                TableData()
                    Text("")
                TableData()
                    Directive(cmd)
                        TextDirective("e(sample)")
                TableData()
                    TextDirective("marks estimation sample")
    Heading('level': 1, 'id': 'contact')
        TextDirective("Author")
    Heading('level': 1, 'id': 'user_guide')
        TextDirective("User Guide")
    Heading('level': 1, 'id': 'updates')
        TextDirective("Latest Updates")
    Heading('level': 1, 'id': 'acknowledgements')
        TextDirective("Acknowledgements")
    Heading('level': 1, 'id': 'references')
        TextDirective("References")
    Para('margins': [0, 0, 2])
        TextDirective("The algorithm underlying reghdfe is a generalization of the works by:")
        Directive(phang)
        TextDirective("Paulo Guimaraes and Pedro Portugal. "A Simple Feasible Alternative Procedure to Estimate")
        TextDirective("Models with High-Dimensional Fixed Effects".")
        Directive(it)
            TextDirective("Stata Journal, 10(4), 628-649, 2010.")
        Directive(browse; "http)
            TextDirective("//www.stata-journal.com/article.html?article=st0212":[link]")
    Para('margins': [0, 0, 2])
        TextDirective("It addresses many of the limitation of previous works, such as possible lack of convergence, arbitrary slow convergence times,")
        TextDirective("and being limited to only two or three sets of fixed effects (for the first paper).")
        TextDirective("The paper explaining the specifics of the algorithm is a work-in-progress and available upon request.")
        Directive(p; 0 0 0)
        TextDirective("If you use this program in your research, please cite either")
        TextDirective("the ")
        Directive(browse; "https)
            TextDirective("//ideas.repec.org/c/boc/bocode/s457874.html":REPEC entry")
        TextDirective("or the aforementioned papers.")
    Heading('level': 1)
        TextDirective("Additional References")
    Para('margins': [0, 0, 0])
        TextDirective("For details on the Aitken acceleration technique employed, please see "method 3" as described by:")
        Directive(phang)
        TextDirective("Macleod, Allan J. "Acceleration of vector sequences by multi-dimensional Delta-2 methods."")
        Directive(it)
            TextDirective("Communications in Applied Numerical Methods 2.4 (1986): 385-392.")
    Para('margins': [0, 0, 0])
        TextDirective("For the rationale behind interacting fixed effects with continuous variables, see:")
        Directive(phang)
        TextDirective("Duflo, Esther. "The medium run effects of educational expansion: Evidence from a large school construction program in Indonesia."")
        Directive(it)
            TextDirective("Journal of Development Economics 74.1 (2004): 163-197.")
        Directive(browse; "http)
            TextDirective("//www.sciencedirect.com/science/article/pii/S0304387803001846": [link]")
    Para('margins': [0, 0, 0])
        TextDirective("Also see:")
        Directive(phang)
        TextDirective("Abowd, J. M., R. H. Creecy, and F. Kramarz 2002.")
        TextDirective("Computing person and firm effects using linked longitudinal employer-employee data.")
        Directive(it)
            TextDirective("Census Bureau Technical Paper TP-2002-06.")
Tags: 1459
Directives: 827
Ratio of done: 0.433
